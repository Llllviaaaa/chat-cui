---
phase: 01-gateway-auth-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - pc-agent/src/main/java/com/chatcui/agent/config/AuthCredentialState.java
  - pc-agent/src/main/java/com/chatcui/agent/config/AuthConfigLoader.java
  - pc-agent/src/main/java/com/chatcui/agent/auth/CredentialProvider.java
  - pc-agent/src/main/java/com/chatcui/agent/auth/WindowsKeystoreCredentialProvider.java
  - pc-agent/src/test/java/com/chatcui/agent/config/AuthConfigLoaderTest.java
  - pc-agent/src/test/java/com/chatcui/agent/auth/WindowsKeystoreCredentialProviderTest.java
  - pc-agent/src/test/java/com/chatcui/agent/config/AuthCredentialStateTest.java
autonomous: true
requirements:
  - AUT-01
must_haves:
  truths:
    - Client runtime can configure tenant/app AK identity and resolve SK from secure keystore-backed storage.
    - Credential lifecycle states Active, Disabled, and Rotating are validated before connection attempt.
    - Logs and config serialization never expose plaintext SK material.
  artifacts:
    - path: pc-agent/src/main/java/com/chatcui/agent/config/AuthConfigLoader.java
      provides: Loads AK/tenant/client metadata and secure SK reference from runtime config.
      contains: validation, redaction, normalization
    - path: pc-agent/src/main/java/com/chatcui/agent/auth/WindowsKeystoreCredentialProvider.java
      provides: SK fetch and update operations backed by Windows credential/keystore APIs.
      contains: secure read/write, no plaintext persistence
    - path: pc-agent/src/main/java/com/chatcui/agent/config/AuthCredentialState.java
      provides: Lifecycle state enum used by preflight checks.
      contains: ACTIVE, DISABLED, ROTATING
  key_links:
    - from: pc-agent/src/main/java/com/chatcui/agent/config/AuthConfigLoader.java
      to: pc-agent/src/main/java/com/chatcui/agent/auth/CredentialProvider.java
      via: Resolve secret handle to concrete provider fetch
      pattern: load.*provider.*secret
    - from: pc-agent/src/main/java/com/chatcui/agent/config/AuthCredentialState.java
      to: pc-agent/src/main/java/com/chatcui/agent/config/AuthConfigLoader.java
      via: Pre-connection state gate
      pattern: ACTIVE|DISABLED|ROTATING
---

<objective>
Implement secure client-side AK/SK configuration and lifecycle handling needed for authenticated gateway connection startup.

Purpose: Satisfy AUT-01 with a production-safe credential loading path that enforces lifecycle state checks.
Output: Client config loader, keystore-backed credential provider, and redaction/lifecycle tests.
</objective>

<execution_context>
@C:/Users/15721/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/15721/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-gateway-auth-foundation/01-01-PLAN.md
@docs/auth/aksk-signature-spec.md
@docs/auth/auth-error-contract.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build credential config schema and validated loader</name>
  <files>pc-agent/src/main/java/com/chatcui/agent/config/AuthCredentialState.java, pc-agent/src/main/java/com/chatcui/agent/config/AuthConfigLoader.java</files>
  <behavior>
    - Valid config returns tenant_id, client_id, ak, secret_ref, and state.
    - Missing tenant_id/client_id/ak/secret_ref is rejected before connection.
    - Secret values are redacted in logs and toString serialization.
  </behavior>
  <action>Create an AuthCredentialState enum (ACTIVE/DISABLED/ROTATING) and implement AuthConfigLoader that validates required fields, normalizes identity fields, and emits redacted diagnostics only. Enforce Disabled as hard-stop and Rotating as allowed-with-warning for phase 1.</action>
  <verify>
    <automated>./mvnw -pl pc-agent -Dtest=AuthConfigLoaderTest,AuthCredentialStateTest test</automated>
  </verify>
  <done>Loader rejects invalid config deterministically and never returns or logs plaintext SK material.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement secure keystore credential provider</name>
  <files>pc-agent/src/main/java/com/chatcui/agent/auth/CredentialProvider.java, pc-agent/src/main/java/com/chatcui/agent/auth/WindowsKeystoreCredentialProvider.java</files>
  <behavior>
    - Provider fetches SK using secret_ref and returns bytes/string only in-memory.
    - Missing or inaccessible keystore entry yields typed auth setup error.
    - Secret update path supports rotation by replacing the referenced key.
  </behavior>
  <action>Define CredentialProvider interface and implement WindowsKeystoreCredentialProvider for secure SK read/update. Ensure plaintext secret is never written to file-based config and use explicit exceptions for not-found, access-denied, and corrupt-entry cases.</action>
  <verify>
    <automated>./mvnw -pl pc-agent -Dtest=WindowsKeystoreCredentialProviderTest test</automated>
  </verify>
  <done>Provider handles secure retrieval and rotation with deterministic error handling for setup failures.</done>
</task>

<task type="auto">
  <name>Task 3: Add client auth bootstrap tests for redaction and lifecycle gates</name>
  <files>pc-agent/src/test/java/com/chatcui/agent/config/AuthConfigLoaderTest.java, pc-agent/src/test/java/com/chatcui/agent/auth/WindowsKeystoreCredentialProviderTest.java, pc-agent/src/test/java/com/chatcui/agent/config/AuthCredentialStateTest.java</files>
  <action>Create focused tests for valid load path, missing-field rejection, disabled-state block, rotating-state allow-with-warning, and redaction guarantees. Include assertions that logs/messages do not contain raw secret values or signature material.</action>
  <verify>
    <automated>./mvnw -pl pc-agent test -Dtest=AuthConfigLoaderTest,WindowsKeystoreCredentialProviderTest,AuthCredentialStateTest</automated>
  </verify>
  <done>Client-side auth bootstrap behavior is reproducible through tests and demonstrates AUT-01 compliance.</done>
</task>

</tasks>

<verification>
Confirm client bootstrap can produce validated AK/SK auth input for gateway requests without leaking sensitive secret data.
</verification>

<success_criteria>
AK/SK config is securely loadable with lifecycle gating and redaction guarantees, fully covered by automated tests.
</success_criteria>

<output>
After completion, create `.planning/phases/01-gateway-auth-foundation/01-02-SUMMARY.md`
</output>
