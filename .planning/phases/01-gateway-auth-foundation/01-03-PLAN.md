---
phase: 01-gateway-auth-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - gateway/src/main/java/com/chatcui/gateway/auth/AuthService.java
  - gateway/src/main/java/com/chatcui/gateway/auth/ReplayGuard.java
  - gateway/src/main/java/com/chatcui/gateway/auth/FailureCooldownPolicy.java
  - gateway/src/main/java/com/chatcui/gateway/auth/ErrorResponseFactory.java
  - gateway/src/main/java/com/chatcui/gateway/http/AuthEntryInterceptor.java
  - gateway/src/main/java/com/chatcui/gateway/ws/WsAuthHandshakeInterceptor.java
  - gateway/src/test/java/com/chatcui/gateway/auth/AuthServiceTest.java
  - gateway/src/test/java/com/chatcui/gateway/auth/ErrorResponseFactoryTest.java
  - gateway/src/test/java/com/chatcui/gateway/ws/WsAuthHandshakeInterceptorTest.java
autonomous: true
requirements:
  - AUT-02
  - AUT-03
must_haves:
  truths:
    - Gateway rejects session establishment whenever AK/SK data is missing, invalid, replayed, disabled, or cooldown-limited.
    - HTTP and WebSocket entry points emit the same AUTH_V1_* failure semantics and required response fields.
    - Successful auth attaches tenant/client principal context before session creation proceeds.
  artifacts:
    - path: gateway/src/main/java/com/chatcui/gateway/auth/AuthService.java
      provides: Ordered auth pipeline (parse, lookup, signature, skew, replay, cooldown, principal attach).
      contains: validate and authenticate entry requests
    - path: gateway/src/main/java/com/chatcui/gateway/auth/ErrorResponseFactory.java
      provides: Deterministic auth error envelope for HTTP and WS mappings.
      contains: error_code, message, next_action, retry_after, trace_id, session_id, debug_id
    - path: gateway/src/main/java/com/chatcui/gateway/ws/WsAuthHandshakeInterceptor.java
      provides: WS handshake enforcement using shared auth pipeline.
      contains: close reason mapping
  key_links:
    - from: gateway/src/main/java/com/chatcui/gateway/http/AuthEntryInterceptor.java
      to: gateway/src/main/java/com/chatcui/gateway/auth/AuthService.java
      via: Pre-session auth guard invocation
      pattern: authService.authenticate
    - from: gateway/src/main/java/com/chatcui/gateway/ws/WsAuthHandshakeInterceptor.java
      to: gateway/src/main/java/com/chatcui/gateway/auth/ErrorResponseFactory.java
      via: Unified AUTH_V1_* transport mapping
      pattern: AUTH_V1_
    - from: gateway/src/main/java/com/chatcui/gateway/auth/AuthService.java
      to: gateway/src/main/java/com/chatcui/gateway/auth/ReplayGuard.java
      via: Nonce replay prevention check
      pattern: replay
---

<objective>
Implement gateway-side authentication enforcement for connection entry with deterministic failures and shared HTTP/WS behavior.

Purpose: Deliver AUT-02 and AUT-03 by ensuring invalid auth requests are blocked before session creation.
Output: Shared auth pipeline, transport adapters, cooldown/replay guards, and failure mapping tests.
</objective>

<execution_context>
@C:/Users/15721/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/15721/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-gateway-auth-foundation/01-01-PLAN.md
@docs/auth/aksk-signature-spec.md
@docs/auth/auth-error-contract.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build ordered gateway auth pipeline service</name>
  <files>gateway/src/main/java/com/chatcui/gateway/auth/AuthService.java, gateway/src/main/java/com/chatcui/gateway/auth/ReplayGuard.java, gateway/src/main/java/com/chatcui/gateway/auth/FailureCooldownPolicy.java</files>
  <behavior>
    - Valid signed request authenticates and returns principal context.
    - Invalid signature, replay nonce, excessive skew, disabled credential, and cooldown state all fail with typed reasons.
    - Pipeline order is fixed and rejects before session allocation.
  </behavior>
  <action>Implement AuthService with explicit stage order: extract, credential lookup, signature verify, skew check, replay check, cooldown check, principal build. Use Redis-backed ReplayGuard and cooldown counters keyed by tenant_id + client_id + ak. Return structured internal failure objects mapped to AuthFailureCode.</action>
  <verify>
    <automated>./mvnw -pl gateway -Dtest=AuthServiceTest test</automated>
  </verify>
  <done>AuthService enforces all required checks and emits deterministic internal failure reasons for translator use.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire shared auth pipeline into HTTP and WS entry interceptors</name>
  <files>gateway/src/main/java/com/chatcui/gateway/http/AuthEntryInterceptor.java, gateway/src/main/java/com/chatcui/gateway/ws/WsAuthHandshakeInterceptor.java, gateway/src/main/java/com/chatcui/gateway/auth/ErrorResponseFactory.java</files>
  <behavior>
    - HTTP auth failure returns mapped status and AUTH_V1_* response body.
    - WS auth failure closes handshake/session with mapped typed reason.
    - Successful HTTP/WS auth paths both attach principal metadata.
  </behavior>
  <action>Integrate AuthService with both interceptors and implement ErrorResponseFactory that translates internal failures to the public schema with safe message, next_action, optional retry_after, and required diagnostic IDs. Ensure no response field includes AK fragment or raw signature data.</action>
  <verify>
    <automated>./mvnw -pl gateway -Dtest=ErrorResponseFactoryTest,WsAuthHandshakeInterceptorTest test</automated>
  </verify>
  <done>Both transports use one failure translator and block unauthenticated session startup consistently.</done>
</task>

<task type="auto">
  <name>Task 3: Add gateway auth enforcement tests covering negative and success paths</name>
  <files>gateway/src/test/java/com/chatcui/gateway/auth/AuthServiceTest.java, gateway/src/test/java/com/chatcui/gateway/auth/ErrorResponseFactoryTest.java, gateway/src/test/java/com/chatcui/gateway/ws/WsAuthHandshakeInterceptorTest.java</files>
  <action>Create tests for missing AK/SK, invalid signature, replay nonce, skew beyond tolerance, cooldown active, disabled credential, and successful auth. Assert expected AUTH_V1_* codes, required response fields, retry_after behavior, and data redaction guarantees.</action>
  <verify>
    <automated>./mvnw -pl gateway test -Dtest=AuthServiceTest,ErrorResponseFactoryTest,WsAuthHandshakeInterceptorTest</automated>
  </verify>
  <done>Gateway auth behavior is fully test-covered for AUT-02/AUT-03, including deterministic HTTP/WS mapping.</done>
</task>

</tasks>

<verification>
Confirm no unauthenticated or invalidly authenticated connection path can proceed to session establishment for either HTTP or WS entry.
</verification>

<success_criteria>
Gateway enforces AK/SK validation at connection entry and emits deterministic explicit failures that clients can act on.
</success_criteria>

<output>
After completion, create `.planning/phases/01-gateway-auth-foundation/01-03-SUMMARY.md`
</output>
