---
phase: 01-gateway-auth-foundation
plan: 06
type: execute
wave: 4
depends_on:
  - "01"
gap_closure: true
files_modified:
  - gateway/src/test/java/com/chatcui/gateway/auth/model/AuthErrorContractDocSyncTest.java
  - gateway/src/test/java/com/chatcui/gateway/auth/model/AuthFailureCodeContractTest.java
  - docs/auth/auth-error-contract.md
autonomous: true
requirements:
  - AUT-03
must_haves:
  truths:
    - Auth error-code tests verify consistency against `docs/auth/auth-error-contract.md`, not only hardcoded in-test maps.
    - Drift between `AuthFailureCode` enum and documented AUTH_V1 table fails tests automatically.
    - Required envelope fields stay enforced for every documented AUTH_V1 code.
  artifacts:
    - path: gateway/src/test/java/com/chatcui/gateway/auth/model/AuthErrorContractDocSyncTest.java
      provides: Markdown-driven doc-to-enum consistency checks.
      contains: AUTH_V1 table parser and assertions
    - path: gateway/src/test/java/com/chatcui/gateway/auth/model/AuthFailureCodeContractTest.java
      provides: Contract guard now references doc-driven mapping helper.
      contains: prefix/uniqueness/required-fields assertions
    - path: docs/auth/auth-error-contract.md
      provides: Stable source for AUTH_V1 mapping consumed by tests.
      contains: code registry + required envelope definitions
  key_links:
    - from: gateway/src/test/java/com/chatcui/gateway/auth/model/AuthErrorContractDocSyncTest.java
      to: docs/auth/auth-error-contract.md
      via: Parse and validate AUTH_V1 entries from docs table
      pattern: AUTH_V1_
    - from: gateway/src/test/java/com/chatcui/gateway/auth/model/AuthFailureCodeContractTest.java
      to: gateway/src/main/java/com/chatcui/gateway/auth/model/AuthFailureCode.java
      via: Enum and docs synchronization guard
      pattern: AUTH_V1_
---

<objective>
Close the contract-drift verification gap by making auth contract tests assert directly against documented AUTH_V1 mappings.

Purpose: Prevent silent divergence between `AuthFailureCode` enum and `docs/auth/auth-error-contract.md`.
Output: Doc-sync test + updated contract guard to enforce code/field parity.
</objective>

<execution_context>
@C:/Users/15721/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/15721/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-gateway-auth-foundation/01-VERIFICATION.md
@.planning/phases/01-gateway-auth-foundation/01-01-PLAN.md
@docs/auth/auth-error-contract.md
@gateway/src/main/java/com/chatcui/gateway/auth/model/AuthFailureCode.java
@gateway/src/test/java/com/chatcui/gateway/auth/model/AuthFailureCodeContractTest.java
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add markdown-driven AUTH_V1 contract parser test</name>
  <files>gateway/src/test/java/com/chatcui/gateway/auth/model/AuthErrorContractDocSyncTest.java</files>
  <behavior>
    - Reads `docs/auth/auth-error-contract.md`.
    - Extracts AUTH_V1 code registry and mapping rows.
    - Asserts doc contains each enum value and no unknown AUTH_V1 code.
  </behavior>
  <action>Create a focused doc-sync test that parses markdown tables and validates enum/document parity.</action>
  <verify>
    <automated>mvn -pl gateway "-Dtest=AuthErrorContractDocSyncTest" test</automated>
  </verify>
  <done>Doc-sync test fails on any AUTH_V1 enum/document mismatch.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor AuthFailureCodeContractTest to consume doc-driven mapping</name>
  <files>gateway/src/test/java/com/chatcui/gateway/auth/model/AuthFailureCodeContractTest.java</files>
  <behavior>
    - Keeps uniqueness and prefix assertions.
    - Replaces hardcoded mapping source with parser/helper fed by docs table.
    - Retains required response field checks (`error_code`, `message`, `next_action`, `trace_id`, `session_id`, `debug_id`).
  </behavior>
  <action>Update contract guard test to eliminate static hardcoded map drift risk.</action>
  <verify>
    <automated>mvn -pl gateway "-Dtest=AuthFailureCodeContractTest,AuthErrorContractDocSyncTest" test</automated>
  </verify>
  <done>Contract guard derives mapping from docs and remains green.</done>
</task>

<task type="auto">
  <name>Task 3: Normalize docs table format for deterministic parsing</name>
  <files>docs/auth/auth-error-contract.md</files>
  <action>Adjust markdown table formatting only as needed to keep parser deterministic while preserving current semantics and values.</action>
  <verify>
    <automated>rg "AUTH_V1_" docs/auth/auth-error-contract.md && mvn -pl gateway "-Dtest=AuthFailureCodeContractTest,AuthErrorContractDocSyncTest" test</automated>
  </verify>
  <done>Docs remain authoritative and machine-validated without semantic drift.</done>
</task>

</tasks>

<verification>
Confirm drift between AUTH_V1 docs and enum cannot pass CI undetected.
</verification>

<success_criteria>
Doc-sync and contract-guard tests enforce consistent AUTH_V1 error mapping and required response fields.
</success_criteria>

<output>
After completion, create `.planning/phases/01-gateway-auth-foundation/01-06-SUMMARY.md`
</output>
