---
phase: 01-gateway-auth-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/auth/aksk-signature-spec.md
  - docs/auth/auth-error-contract.md
  - gateway/src/main/java/com/chatcui/gateway/auth/model/AuthPrincipal.java
  - gateway/src/main/java/com/chatcui/gateway/auth/model/AuthFailureCode.java
  - gateway/src/main/resources/auth-defaults.yml
  - gateway/src/test/java/com/chatcui/gateway/auth/model/AuthFailureCodeContractTest.java
autonomous: true
requirements:
  - AUT-02
  - AUT-03
must_haves:
  truths:
    - Gateway authentication uses one canonical AK/SK signature contract for all entry paths.
    - Every auth rejection maps to a stable AUTH_V1_* code with deterministic response fields.
    - TTL, renewal ratio, skew tolerance, replay TTL, and cooldown defaults are explicit and configurable.
  artifacts:
    - path: docs/auth/aksk-signature-spec.md
      provides: Canonical signing field order, encoding, and replay key namespace.
      contains: canonical string, timestamp/nonce validation
    - path: docs/auth/auth-error-contract.md
      provides: Versioned AUTH_V1_* taxonomy and HTTP/WS mapping table.
      contains: error_code, message, next_action, retry_after, trace_id, session_id, debug_id
    - path: gateway/src/main/java/com/chatcui/gateway/auth/model/AuthFailureCode.java
      provides: Strongly typed failure codes shared by translators.
      contains: AUTH_V1_
    - path: gateway/src/main/resources/auth-defaults.yml
      provides: Centralized baseline policy values.
      contains: ttl, renewal_ratio, skew_tolerance, replay_ttl, cooldown
  key_links:
    - from: docs/auth/aksk-signature-spec.md
      to: gateway/src/main/java/com/chatcui/gateway/auth/model/AuthPrincipal.java
      via: Required principal fields contract
      pattern: tenant_id|client_id|trace_id|session_id
    - from: gateway/src/main/java/com/chatcui/gateway/auth/model/AuthFailureCode.java
      to: docs/auth/auth-error-contract.md
      via: Error code registry synchronization
      pattern: AUTH_V1_
---

<objective>
Establish the phase-1 authentication contracts before implementation starts.

Purpose: Lock signatures, error schema, and policy defaults so client and gateway implementation remains deterministic.
Output: Auth spec docs, failure code model, auth defaults, and a contract guard test.
</objective>

<execution_context>
@C:/Users/15721/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/15721/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-gateway-auth-foundation/01-CONTEXT.md
@.planning/phases/01-gateway-auth-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Freeze AK/SK signature and anti-replay spec</name>
  <files>docs/auth/aksk-signature-spec.md, docs/auth/auth-error-contract.md</files>
  <action>Document canonical signature fields and order (ak, tenant_id, client_id, timestamp, nonce, session_id), canonical encoding rules, accepted skew window policy, replay key namespace, and redaction rules. Define AUTH_V1_* namespace with stage-specific codes (missing credential, invalid signature, replay detected, cooldown active, credential disabled, permission denied) and map each to HTTP/WS behavior plus next_action/retry_after guidance.</action>
  <verify>
    <automated>rg "AUTH_V1_" docs/auth/auth-error-contract.md && rg "canonical" docs/auth/aksk-signature-spec.md</automated>
  </verify>
  <done>Both documents exist with complete canonicalization and error mapping tables aligned to phase decisions.</done>
</task>

<task type="auto">
  <name>Task 2: Create gateway auth contract models and baseline policy config</name>
  <files>gateway/src/main/java/com/chatcui/gateway/auth/model/AuthPrincipal.java, gateway/src/main/java/com/chatcui/gateway/auth/model/AuthFailureCode.java, gateway/src/main/resources/auth-defaults.yml</files>
  <action>Create AuthPrincipal with tenant_id, client_id, trace_id, session_id, credential_state, and issued_at fields. Create AuthFailureCode enum containing all AUTH_V1_* values from Task 1. Add auth-defaults.yml with default TTL/renewal/skew/replay/cooldown values and comments indicating which values are tunable.</action>
  <verify>
    <automated>./mvnw -pl gateway -DskipTests compile</automated>
  </verify>
  <done>Gateway module compiles with the new model/config artifacts and no hardcoded magic auth constants outside defaults.</done>
</task>

<task type="auto">
  <name>Task 3: Add contract guard test for error code and response fields</name>
  <files>gateway/src/test/java/com/chatcui/gateway/auth/model/AuthFailureCodeContractTest.java</files>
  <action>Add a focused test that asserts every public AUTH_V1_* code is unique, version-prefixed, and represented in the error contract mapping table (including required response fields: error_code, message, next_action, trace_id, session_id, debug_id).</action>
  <verify>
    <automated>./mvnw -pl gateway -Dtest=AuthFailureCodeContractTest test</automated>
  </verify>
  <done>The guard test passes and fails if codes drift from the documented contract.</done>
</task>

</tasks>

<verification>
Confirm the spec and code model can be consumed by both client and gateway plans without introducing alternate auth/error formats.
</verification>

<success_criteria>
Canonical signature contract, deterministic AUTH_V1_* schema, and baseline policy defaults are implemented and test-guarded.
</success_criteria>

<output>
After completion, create `.planning/phases/01-gateway-auth-foundation/01-01-SUMMARY.md`
</output>
