---
phase: 01-gateway-auth-foundation
plan: 05
type: execute
wave: 4
depends_on:
  - "02"
  - "04"
gap_closure: true
files_modified:
  - pc-agent/pom.xml
  - pc-agent/src/main/java/com/chatcui/agent/auth/CredentialProvider.java
  - pc-agent/src/main/java/com/chatcui/agent/auth/WindowsCredentialStore.java
  - pc-agent/src/main/java/com/chatcui/agent/auth/WindowsKeystoreCredentialProvider.java
  - pc-agent/src/test/java/com/chatcui/agent/auth/WindowsCredentialStoreTest.java
  - pc-agent/src/test/java/com/chatcui/agent/auth/WindowsKeystoreCredentialProviderTest.java
  - pc-agent/src/test/java/com/chatcui/agent/integration/ReauthOnReconnectIntegrationTest.java
autonomous: true
requirements:
  - AUT-01
must_haves:
  truths:
    - SK persistence is no longer an in-memory placeholder and uses a Windows credential/keystore backend abstraction.
    - Credential provider failures are typed and deterministic for not-found, access-denied, and corrupt-entry scenarios.
    - Existing reconnect/auth bootstrap tests still pass after storage backend upgrade.
  artifacts:
    - path: pc-agent/src/main/java/com/chatcui/agent/auth/WindowsCredentialStore.java
      provides: OS credential-manager abstraction for storing and fetching secret material.
      contains: read, write, delete
    - path: pc-agent/src/main/java/com/chatcui/agent/auth/WindowsKeystoreCredentialProvider.java
      provides: Production credential provider backed by Windows credential store abstraction.
      contains: typed errors, no in-memory map-backed secrets
    - path: pc-agent/src/test/java/com/chatcui/agent/auth/WindowsCredentialStoreTest.java
      provides: Contract tests for store read/write/error semantics.
      contains: not-found, access-denied, corrupt-entry
  key_links:
    - from: pc-agent/src/main/java/com/chatcui/agent/auth/WindowsKeystoreCredentialProvider.java
      to: pc-agent/src/main/java/com/chatcui/agent/auth/CredentialProvider.java
      via: Interface conformance for runtime secret resolution
      pattern: readSecret|upsertSecret
    - from: pc-agent/src/test/java/com/chatcui/agent/integration/ReauthOnReconnectIntegrationTest.java
      to: pc-agent/src/main/java/com/chatcui/agent/auth/WindowsKeystoreCredentialProvider.java
      via: Reconnect re-auth uses non-placeholder secret backend
      pattern: secret_ref|readSecret
---

<objective>
Close the AUT-01 security gap by replacing placeholder in-memory secret storage with a real Windows credential backend abstraction.

Purpose: Ensure phase verification no longer flags "ConcurrentHashMap placeholder" and that pc-agent secret storage behavior aligns with auth foundation security intent.
Output: Windows credential store abstraction + provider integration + tests proving typed error behavior and reconnect compatibility.
</objective>

<execution_context>
@C:/Users/15721/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/15721/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-gateway-auth-foundation/01-VERIFICATION.md
@.planning/phases/01-gateway-auth-foundation/01-02-PLAN.md
@pc-agent/src/main/java/com/chatcui/agent/auth/CredentialProvider.java
@pc-agent/src/main/java/com/chatcui/agent/auth/WindowsKeystoreCredentialProvider.java
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Introduce Windows credential-store abstraction</name>
  <files>pc-agent/src/main/java/com/chatcui/agent/auth/WindowsCredentialStore.java, pc-agent/src/test/java/com/chatcui/agent/auth/WindowsCredentialStoreTest.java</files>
  <behavior>
    - Provides read/write/delete by secret_ref alias.
    - Throws typed errors for not-found/access-denied/corrupt-entry.
    - Does not expose raw secret data in toString/loggable metadata.
  </behavior>
  <action>Create a credential-store abstraction suitable for Windows host runtime and cover contract behavior with focused tests.</action>
  <verify>
    <automated>mvn -pl pc-agent -am "-Dtest=WindowsCredentialStoreTest" "-Dsurefire.failIfNoSpecifiedTests=false" test</automated>
  </verify>
  <done>Credential-store abstraction is available and test-covered with deterministic typed failures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace provider placeholder storage with credential-store backend</name>
  <files>pc-agent/src/main/java/com/chatcui/agent/auth/WindowsKeystoreCredentialProvider.java, pc-agent/src/main/java/com/chatcui/agent/auth/CredentialProvider.java, pc-agent/src/test/java/com/chatcui/agent/auth/WindowsKeystoreCredentialProviderTest.java</files>
  <behavior>
    - Provider delegates persistence operations to WindowsCredentialStore.
    - No in-memory map is used as secret source of truth.
    - Error translation remains stable for callers.
  </behavior>
  <action>Refactor provider implementation to use the new credential-store abstraction and adjust tests accordingly.</action>
  <verify>
    <automated>mvn -pl pc-agent -am "-Dtest=WindowsKeystoreCredentialProviderTest" "-Dsurefire.failIfNoSpecifiedTests=false" test</automated>
  </verify>
  <done>Provider no longer contains map-backed placeholder logic and all provider tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Re-run auth bootstrap and reconnect evidence on updated provider</name>
  <files>pc-agent/src/test/java/com/chatcui/agent/config/AuthConfigLoaderTest.java, pc-agent/src/test/java/com/chatcui/agent/integration/ReauthOnReconnectIntegrationTest.java</files>
  <action>Revalidate phase-1 AUT-01 evidence against the updated provider backend and ensure reconnect re-auth still behaves deterministically.</action>
  <verify>
    <automated>mvn -pl pc-agent -am "-Dtest=AuthConfigLoaderTest,ReauthOnReconnectIntegrationTest" "-Dsurefire.failIfNoSpecifiedTests=false" test</automated>
  </verify>
  <done>All AUT-01 bootstrap and reconnect tests pass with non-placeholder secret storage implementation.</done>
</task>

</tasks>

<verification>
Confirm the verification gap on placeholder credential storage is closed and cannot regress silently.
</verification>

<success_criteria>
Windows keystore provider no longer uses map-backed placeholder persistence and AUT-01 evidence remains green.
</success_criteria>

<output>
After completion, create `.planning/phases/01-gateway-auth-foundation/01-05-SUMMARY.md`
</output>
