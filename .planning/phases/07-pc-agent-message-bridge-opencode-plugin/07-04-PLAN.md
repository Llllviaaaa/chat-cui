---
phase: 07-pc-agent-message-bridge-opencode-plugin
plan: 04
type: execute
wave: 4
depends_on:
  - "01"
  - "02"
  - "03"
gap_closure: true
files_modified:
  - .planning/phases/07-pc-agent-message-bridge-opencode-plugin/07-ACCEPTANCE-EVIDENCE.md
  - .planning/phases/07-pc-agent-message-bridge-opencode-plugin/07-VERIFICATION.md
  - .planning/phases/07-pc-agent-message-bridge-opencode-plugin/07-GAP-CLOSED-CHECKLIST.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - P07-BASE-01
  - P07-REF-01
  - P07-COMPAT-01
  - P07-VERSION-01
  - P07-GATE-01
  - P07-EVID-01
  - P07-GOV-01
must_haves:
  truths:
    - Acceptance evidence captures command, execution date, result, and correlation identifiers (`session_id`, `trace_id`) for every hard-gate component.
    - `P07-REF-01` closure requires populated baseline snapshot proof fields: `reference_repo`, `reference_tag_or_commit`, `snapshot_date_utc`, and `diff_scope`, logged under `evidence_id: EVID-P07-REF-01-BASELINE`.
    - Verification report closes each `P07-*` requirement by linking truth, artifact, key-link, and executed proof.
    - Gap checklist is fully closed or waiver-backed with non-expired governance metadata.
    - Planning trackers are synchronized so Phase 07 status and requirement traceability are consistent across roadmap/state/requirements.
  artifacts:
    - path: .planning/phases/07-pc-agent-message-bridge-opencode-plugin/07-ACCEPTANCE-EVIDENCE.md
      provides: reproducible proof log for plugin+gateway hard-gate execution.
      contains: command/date/result/session_id/trace_id records plus `P07-REF-01` baseline snapshot proof fields
    - path: .planning/phases/07-pc-agent-message-bridge-opencode-plugin/07-VERIFICATION.md
      provides: goal-backward requirement closure decision for all `P07-*` IDs.
      contains: must-have checks, explicit `P07-REF-01 -> EVID-P07-REF-01-BASELINE` citation, and final pass/fail rationale
    - path: .planning/phases/07-pc-agent-message-bridge-opencode-plugin/07-GAP-CLOSED-CHECKLIST.md
      provides: final gap status table with evidence and waiver references.
      contains: all closure rows marked closed or explicitly waived with expiry
    - path: .planning/ROADMAP.md
      provides: phase completion and plan checklist synchronization.
      contains: Phase 07 marked complete with executed plan references
    - path: .planning/STATE.md
      provides: session continuity and next-focus handoff after phase closure.
      contains: Phase 07 completion note and milestone status
  key_links:
    - from: .planning/phases/07-pc-agent-message-bridge-opencode-plugin/07-ACCEPTANCE-EVIDENCE.md
      to: .planning/phases/07-pc-agent-message-bridge-opencode-plugin/07-VERIFICATION.md
      via: requirement-to-proof citations
      pattern: each `P07-*` requirement has explicit evidence references and `P07-REF-01` cites `EVID-P07-REF-01-BASELINE`
    - from: .planning/phases/07-pc-agent-message-bridge-opencode-plugin/07-VERIFICATION.md
      to: .planning/ROADMAP.md
      via: phase status decision
      pattern: verification pass drives tracker completion updates
---

<objective>
Complete Phase 07 with auditable evidence, requirement-level verification, and synchronized planning trackers.

Purpose: Convert alignment/gate/governance work into a final closure decision that is reproducible and traceable.
Output: acceptance evidence, verification report, gap checklist closeout, and tracker updates.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-pc-agent-message-bridge-opencode-plugin/07-REQUIREMENT-MAPPING.md
@.planning/phases/07-pc-agent-message-bridge-opencode-plugin/07-GAP-CLOSED-CHECKLIST.md
@.planning/phases/07-pc-agent-message-bridge-opencode-plugin/07-ALIGNMENT-RUNBOOK.md
@.planning/phases/07-pc-agent-message-bridge-opencode-plugin/07-WAIVER-TEMPLATE.md
@.planning/phases/07-pc-agent-message-bridge-opencode-plugin/07-01-SUMMARY.md
@.planning/phases/07-pc-agent-message-bridge-opencode-plugin/07-02-SUMMARY.md
@.planning/phases/07-pc-agent-message-bridge-opencode-plugin/07-03-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Capture full-chain acceptance evidence with required correlation metadata and reference baseline proof</name>
  <files>.planning/phases/07-pc-agent-message-bridge-opencode-plugin/07-ACCEPTANCE-EVIDENCE.md, .planning/phases/07-pc-agent-message-bridge-opencode-plugin/07-GAP-CLOSED-CHECKLIST.md</files>
  <behavior>
    - Evidence includes all hard-gate command segments and their outputs/status.
    - Every evidence row records execution date plus `session_id` and `trace_id` where available.
    - `P07-REF-01` evidence row stores `reference_repo`, `reference_tag_or_commit`, `snapshot_date_utc`, `diff_scope`, and `evidence_id: EVID-P07-REF-01-BASELINE`.
    - Checklist rows are updated with pass/fail, evidence references, and waiver ids if a temporary exception is used.
  </behavior>
  <action>Execute the phase-07 hard gate, then capture baseline proof for `message-bridge-opencode-plugin` by running and logging snapshot commands (`git remote get-url origin`, `git rev-parse HEAD`, `git show -s --format=%cI HEAD`, `git diff --name-only`) against the selected reference baseline; record `reference_repo`, `reference_tag_or_commit`, `snapshot_date_utc`, `diff_scope`, and `evidence_id: EVID-P07-REF-01-BASELINE` in acceptance evidence; finalize checklist state using governance rules.</action>
  <verify>
    <automated>rg "verify:phase-07|session_id|trace_id|date|result|waiver|EVID-P07-REF-01-BASELINE|reference_repo:|reference_tag_or_commit:|snapshot_date_utc:|diff_scope:|git remote get-url origin|git rev-parse HEAD|git show -s --format=%cI HEAD|git diff --name-only" .planning/phases/07-pc-agent-message-bridge-opencode-plugin/07-ACCEPTANCE-EVIDENCE.md .planning/phases/07-pc-agent-message-bridge-opencode-plugin/07-GAP-CLOSED-CHECKLIST.md && if (Select-String -Path .planning/phases/07-pc-agent-message-bridge-opencode-plugin/07-ACCEPTANCE-EVIDENCE.md -Pattern "reference_repo: TBD|reference_tag_or_commit: TBD|snapshot_date_utc: TBD|diff_scope: TBD" -Quiet) { exit 1 } else { Write-Output "P07-REF-01 baseline snapshot populated" }</automated>
  </verify>
  <done>Acceptance evidence is reproducible, includes executable `P07-REF-01` baseline proof, and checklist closure is audit-ready.</done>
</task>

<task type="auto">
  <name>Task 2: Produce final verification report and synchronize planning trackers</name>
  <files>.planning/phases/07-pc-agent-message-bridge-opencode-plugin/07-VERIFICATION.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/REQUIREMENTS.md</files>
  <behavior>
    - Verification report maps every `P07-*` ID to evidence artifacts, executed commands, and closure verdict.
    - `P07-REF-01` section in verification references `EVID-P07-REF-01-BASELINE` and restates baseline fields (`reference_repo`, `reference_tag_or_commit`, `snapshot_date_utc`, `diff_scope`).
    - `ROADMAP.md`, `STATE.md`, and `REQUIREMENTS.md` consistently reflect Phase 07 completion state.
    - Any non-expired waiver is surfaced explicitly as residual risk in verification notes.
  </behavior>
  <action>Write requirement-level verification with explicit citation chaining (`P07-REF-01 -> EVID-P07-REF-01-BASELINE`) and update top-level trackers to reflect a consistent closure decision.</action>
  <verify>
    <automated>rg "P07-(BASE|REF|COMPAT|VERSION|GATE|EVID|GOV)-01|Phase 7|Complete|Pending|EVID-P07-REF-01-BASELINE|reference_repo|reference_tag_or_commit|snapshot_date_utc|diff_scope" .planning/phases/07-pc-agent-message-bridge-opencode-plugin/07-VERIFICATION.md .planning/ROADMAP.md .planning/STATE.md .planning/REQUIREMENTS.md && if ((Select-String -Path .planning/phases/07-pc-agent-message-bridge-opencode-plugin/07-ACCEPTANCE-EVIDENCE.md -Pattern "evidence_id: EVID-P07-REF-01-BASELINE" -Quiet) -and (Select-String -Path .planning/phases/07-pc-agent-message-bridge-opencode-plugin/07-VERIFICATION.md -Pattern "P07-REF-01" -Quiet) -and (Select-String -Path .planning/phases/07-pc-agent-message-bridge-opencode-plugin/07-VERIFICATION.md -Pattern "EVID-P07-REF-01-BASELINE" -Quiet)) { Write-Output "P07-REF-01 citation chain present" } else { exit 1 }</automated>
  </verify>
  <done>Final verification and tracker state are consistent, traceable, and aligned to phase goals.</done>
</task>
</tasks>

<verification>
- `npm.cmd --prefix pc-agent-plugin run verify:phase-07`
- `rg "verify:phase-07|session_id|trace_id|date|result|waiver|EVID-P07-REF-01-BASELINE|reference_repo:|reference_tag_or_commit:|snapshot_date_utc:|diff_scope:|git remote get-url origin|git rev-parse HEAD|git show -s --format=%cI HEAD|git diff --name-only" .planning/phases/07-pc-agent-message-bridge-opencode-plugin/07-ACCEPTANCE-EVIDENCE.md .planning/phases/07-pc-agent-message-bridge-opencode-plugin/07-GAP-CLOSED-CHECKLIST.md && if (Select-String -Path .planning/phases/07-pc-agent-message-bridge-opencode-plugin/07-ACCEPTANCE-EVIDENCE.md -Pattern "reference_repo: TBD|reference_tag_or_commit: TBD|snapshot_date_utc: TBD|diff_scope: TBD" -Quiet) { exit 1 } else { Write-Output "P07-REF-01 baseline snapshot populated" }`
- `rg "P07-(BASE|REF|COMPAT|VERSION|GATE|EVID|GOV)-01|Phase 7|Complete|Pending|EVID-P07-REF-01-BASELINE|reference_repo|reference_tag_or_commit|snapshot_date_utc|diff_scope" .planning/phases/07-pc-agent-message-bridge-opencode-plugin/07-VERIFICATION.md .planning/ROADMAP.md .planning/STATE.md .planning/REQUIREMENTS.md && if ((Select-String -Path .planning/phases/07-pc-agent-message-bridge-opencode-plugin/07-ACCEPTANCE-EVIDENCE.md -Pattern "evidence_id: EVID-P07-REF-01-BASELINE" -Quiet) -and (Select-String -Path .planning/phases/07-pc-agent-message-bridge-opencode-plugin/07-VERIFICATION.md -Pattern "P07-REF-01" -Quiet) -and (Select-String -Path .planning/phases/07-pc-agent-message-bridge-opencode-plugin/07-VERIFICATION.md -Pattern "EVID-P07-REF-01-BASELINE" -Quiet)) { Write-Output "P07-REF-01 citation chain present" } else { exit 1 }`
</verification>

<success_criteria>
- Every provisional `P07-*` requirement is explicitly verified with linked evidence.
- Evidence records satisfy command/date/result/session_id/trace_id auditability requirements.
- `P07-REF-01` baseline proof is executable and citation-linked from acceptance evidence to verification.
- Planning trackers show a coherent Phase-07 closure state with no unresolved `TBD` requirement metadata.
</success_criteria>

<output>
After completion, create `.planning/phases/07-pc-agent-message-bridge-opencode-plugin/07-04-SUMMARY.md`
</output>
