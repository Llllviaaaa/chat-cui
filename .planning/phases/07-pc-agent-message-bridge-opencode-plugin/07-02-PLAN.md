---
phase: 07-pc-agent-message-bridge-opencode-plugin
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
gap_closure: true
files_modified:
  - pc-agent-plugin/src/core/events/PluginEvents.ts
  - pc-agent-plugin/src/host-adapter/contracts/HostPluginContract.ts
  - pc-agent-plugin/src/host-adapter/HostEventBridge.ts
  - pc-agent-plugin/test/integration/host/HostEventContract.integration.test.ts
  - pc-agent-plugin/test/integration/core/BridgeSessionRuntime.integration.test.ts
  - gateway/src/test/java/com/chatcui/gateway/integration/AuthEntryIntegrationTest.java
  - gateway/src/test/java/com/chatcui/gateway/integration/WsAuthFailureIntegrationTest.java
  - gateway/src/test/java/com/chatcui/gateway/runtime/ResumeCoordinatorTest.java
  - .planning/phases/07-pc-agent-message-bridge-opencode-plugin/07-ALIGNMENT-RUNBOOK.md
autonomous: true
requirements:
  - P07-COMPAT-01
  - P07-VERSION-01
must_haves:
  truths:
    - Key host/runtime event envelopes expose explicit `contract_version` while preserving existing field semantics.
    - Contract changes are additive only; no required field removals or meaning changes are introduced.
    - Gateway auth/resume regression suites remain green, proving compatibility with plugin-side envelope evolution.
    - Compatibility policy and deprecation entry rules are documented in phase runbook before gate assembly.
  artifacts:
    - path: pc-agent-plugin/src/host-adapter/contracts/HostPluginContract.ts
      provides: typed event envelope contract including explicit version signal.
      contains: additive `contract_version` field and backward-compatibility notes
    - path: pc-agent-plugin/src/host-adapter/HostEventBridge.ts
      provides: runtime-to-host event projection that sets version metadata on key events.
      contains: event mapping updates for `runtime.*` and `gateway.*` envelopes
    - path: pc-agent-plugin/test/integration/host/HostEventContract.integration.test.ts
      provides: executable assertions for event schema compatibility and version field presence.
      contains: version signal and backward-compat assertions
    - path: gateway/src/test/java/com/chatcui/gateway/runtime/ResumeCoordinatorTest.java
      provides: regression proof that resume semantics are unchanged by phase-07 alignment work.
      contains: owner/sequence decision contract checks
    - path: .planning/phases/07-pc-agent-message-bridge-opencode-plugin/07-ALIGNMENT-RUNBOOK.md
      provides: additive-only policy and deprecation entry criteria.
      contains: contract evolution guardrails section
  key_links:
    - from: pc-agent-plugin/src/host-adapter/HostEventBridge.ts
      to: pc-agent-plugin/src/host-adapter/contracts/HostPluginContract.ts
      via: host event envelope mapping
      pattern: emitted payload conforms to typed contract with `contract_version`
    - from: pc-agent-plugin/test/integration/host/HostEventContract.integration.test.ts
      to: gateway/src/test/java/com/chatcui/gateway/integration/AuthEntryIntegrationTest.java
      via: cross-boundary compatibility gate
      pattern: plugin envelope changes do not break gateway auth/reject contract paths
---

<objective>
Enforce additive compatibility and explicit version signaling on key plugin-host event contracts without changing user-visible behavior.

Purpose: Close alignment gaps around contract governance and compatibility semantics for plugin/gateway boundaries.
Output: Versioned host event contracts plus regression tests proving backward compatibility is intact.
</objective>

<context>
@.planning/phases/07-pc-agent-message-bridge-opencode-plugin/07-CONTEXT.md
@.planning/phases/07-pc-agent-message-bridge-opencode-plugin/07-RESEARCH.md
@.planning/phases/07-pc-agent-message-bridge-opencode-plugin/07-01-PLAN.md
@pc-agent-plugin/src/core/events/PluginEvents.ts
@pc-agent-plugin/src/host-adapter/contracts/HostPluginContract.ts
@pc-agent-plugin/src/host-adapter/HostEventBridge.ts
@gateway/src/test/java/com/chatcui/gateway/runtime/ResumeCoordinatorTest.java
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add explicit contract-version signaling to key host/runtime event envelopes</name>
  <files>pc-agent-plugin/src/core/events/PluginEvents.ts, pc-agent-plugin/src/host-adapter/contracts/HostPluginContract.ts, pc-agent-plugin/src/host-adapter/HostEventBridge.ts, pc-agent-plugin/test/integration/host/HostEventContract.integration.test.ts, pc-agent-plugin/test/integration/core/BridgeSessionRuntime.integration.test.ts</files>
  <behavior>
    - `runtime.*` and `gateway.*` host-facing events include additive `contract_version` metadata.
    - Existing fields remain present and semantically unchanged to preserve strict backward compatibility.
    - Version value is deterministic and machine-readable for compatibility evaluation in downstream tooling.
  </behavior>
  <action>Extend plugin event typing and host-event projection logic to include explicit version metadata on key envelopes, then update host and bridge-runtime integration tests to assert additive compatibility and version presence.</action>
  <verify>
    <automated>npm.cmd --prefix pc-agent-plugin run test:host-events && npm.cmd --prefix pc-agent-plugin run test:bridge-runtime</automated>
  </verify>
  <done>Host-boundary runtime events are version-signaled and remain backward compatible by contract tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove gateway auth/resume contracts remain stable after plugin contract alignment</name>
  <files>gateway/src/test/java/com/chatcui/gateway/integration/AuthEntryIntegrationTest.java, gateway/src/test/java/com/chatcui/gateway/integration/WsAuthFailureIntegrationTest.java, gateway/src/test/java/com/chatcui/gateway/runtime/ResumeCoordinatorTest.java, .planning/phases/07-pc-agent-message-bridge-opencode-plugin/07-ALIGNMENT-RUNBOOK.md</files>
  <behavior>
    - Gateway auth failure envelopes and resume decision behavior stay unchanged in required fields/semantics.
    - Regression tests run against the same canonical suites used in earlier auth/resume phases.
    - Runbook captures additive-only policy and rules for introducing deprecations without breaking existing contracts.
  </behavior>
  <action>Harden gateway regression assertions where needed and document compatibility policy details in the runbook so future changes have enforceable rules.</action>
  <verify>
    <automated>mvn -pl gateway "-Dtest=AuthServiceTest,ErrorResponseFactoryTest,WsAuthHandshakeInterceptorTest,AuthEntryIntegrationTest,WsAuthFailureIntegrationTest,ResumeCoordinatorTest" test && rg "additive|backward|contract_version|deprecation" .planning/phases/07-pc-agent-message-bridge-opencode-plugin/07-ALIGNMENT-RUNBOOK.md</automated>
  </verify>
  <done>Compatibility guarantees are both test-backed and operationally documented.</done>
</task>
</tasks>

<verification>
- `npm.cmd --prefix pc-agent-plugin run test:host-events`
- `npm.cmd --prefix pc-agent-plugin run test:bridge-runtime`
- `mvn -pl gateway "-Dtest=AuthServiceTest,ErrorResponseFactoryTest,WsAuthHandshakeInterceptorTest,AuthEntryIntegrationTest,WsAuthFailureIntegrationTest,ResumeCoordinatorTest" test`
- `rg "additive|backward|contract_version|deprecation" .planning/phases/07-pc-agent-message-bridge-opencode-plugin/07-ALIGNMENT-RUNBOOK.md`
</verification>

<success_criteria>
- Key plugin host-boundary events expose explicit `contract_version` signal with no breaking schema changes.
- Gateway auth/resume suites prove compatibility remains intact after version-signal alignment.
- Runbook records enforceable additive-only policy constraints for this boundary.
</success_criteria>

<output>
After completion, create `.planning/phases/07-pc-agent-message-bridge-opencode-plugin/07-02-SUMMARY.md`
</output>
