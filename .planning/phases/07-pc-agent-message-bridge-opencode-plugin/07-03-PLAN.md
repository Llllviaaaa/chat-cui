---
phase: 07-pc-agent-message-bridge-opencode-plugin
plan: 03
type: execute
wave: 3
depends_on:
  - "02"
gap_closure: true
files_modified:
  - pc-agent-plugin/package.json
  - .github/workflows/phase-07-alignment-gate.yml
  - .planning/phases/07-pc-agent-message-bridge-opencode-plugin/07-WAIVER-TEMPLATE.md
  - .planning/phases/07-pc-agent-message-bridge-opencode-plugin/07-ALIGNMENT-RUNBOOK.md
  - .planning/phases/07-pc-agent-message-bridge-opencode-plugin/07-GAP-CLOSED-CHECKLIST.md
autonomous: true
requirements:
  - P07-GATE-01
  - P07-GOV-01
must_haves:
  truths:
    - Phase-07 hard gate is consolidated into one repeatable entrypoint that covers plugin no-drift, host integration, CLI real-chain, and gateway auth/resume regressions.
    - CI workflow executes the same hard gate and blocks merge/release when any gate component fails.
    - Waivers are exception-only and require owner, reason, scope, and expiration date.
    - Deprecation policy enforces a two-release window before any removal of previously supported fields/events.
  artifacts:
    - path: pc-agent-plugin/package.json
      provides: local hard-gate command composition entrypoint (`verify:phase-07`).
      contains: plugin + gateway gate command sequence
    - path: .github/workflows/phase-07-alignment-gate.yml
      provides: CI enforcement for phase-07 alignment hard gate.
      contains: fail-fast pipeline running the consolidated gate
    - path: .planning/phases/07-pc-agent-message-bridge-opencode-plugin/07-WAIVER-TEMPLATE.md
      provides: auditable waiver contract for temporary gate bypass.
      contains: owner, approver, expiration, impacted requirements, and rollback plan fields
    - path: .planning/phases/07-pc-agent-message-bridge-opencode-plugin/07-ALIGNMENT-RUNBOOK.md
      provides: governance policy source for waivers and deprecations.
      contains: release-block default and two-release deprecation window
    - path: .planning/phases/07-pc-agent-message-bridge-opencode-plugin/07-GAP-CLOSED-CHECKLIST.md
      provides: checklist enforcement rows tied to hard-gate outcomes and waiver references.
      contains: gate status and waiver linkage columns
  key_links:
    - from: .github/workflows/phase-07-alignment-gate.yml
      to: pc-agent-plugin/package.json
      via: `verify:phase-07` command invocation
      pattern: CI and local gate execute identical checks
    - from: .planning/phases/07-pc-agent-message-bridge-opencode-plugin/07-WAIVER-TEMPLATE.md
      to: .planning/phases/07-pc-agent-message-bridge-opencode-plugin/07-GAP-CLOSED-CHECKLIST.md
      via: waiver reference id
      pattern: any gate bypass has expiring, traceable checklist linkage
---

<objective>
Assemble release-blocking hard-gate automation and governance controls for alignment closure.

Purpose: Make phase-07 compatibility guarantees enforceable by default, with tightly controlled exceptions.
Output: Consolidated gate command + CI workflow + waiver/deprecation governance artifacts.
</objective>

<context>
@.planning/phases/07-pc-agent-message-bridge-opencode-plugin/07-CONTEXT.md
@.planning/phases/07-pc-agent-message-bridge-opencode-plugin/07-RESEARCH.md
@.planning/phases/07-pc-agent-message-bridge-opencode-plugin/07-02-PLAN.md
@pc-agent-plugin/package.json
@.github/workflows/phase-01.1-no-drift.yml
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Compose and enforce consolidated phase-07 hard gate</name>
  <files>pc-agent-plugin/package.json, .github/workflows/phase-07-alignment-gate.yml</files>
  <behavior>
    - `verify:phase-07` runs at least: `verify:no-drift`, `test:plugin-mode-integration`, `test:cli-real-chain`, plus gateway auth/resume regression command.
    - CI workflow runs on pull requests and main-branch pushes for plugin/gateway/phase-07 workflow changes.
    - Workflow fails immediately on any gate failure and reports failed command segment clearly.
  </behavior>
  <action>Add gate command composition and CI workflow so local verification and automation use one canonical hard-gate path.</action>
  <verify>
    <automated>npm.cmd --prefix pc-agent-plugin run verify:phase-07 && rg "verify:phase-07|pull_request|gateway|pc-agent-plugin" .github/workflows/phase-07-alignment-gate.yml</automated>
  </verify>
  <done>A single hard-gate entrypoint is executable locally and enforced in CI by default.</done>
</task>

<task type="auto">
  <name>Task 2: Establish waiver and deprecation governance with checklist enforcement</name>
  <files>.planning/phases/07-pc-agent-message-bridge-opencode-plugin/07-WAIVER-TEMPLATE.md, .planning/phases/07-pc-agent-message-bridge-opencode-plugin/07-ALIGNMENT-RUNBOOK.md, .planning/phases/07-pc-agent-message-bridge-opencode-plugin/07-GAP-CLOSED-CHECKLIST.md</files>
  <behavior>
    - Waiver template requires owner, approver, expiration, affected `P07-*` IDs, and compensating controls.
    - Runbook documents release-block default and two-release deprecation policy for contract removals.
    - Checklist tracks whether each closed gap is gate-passed or waiver-backed with expiry.
  </behavior>
  <action>Publish governance artifacts and cross-link them so any exception is explicit, temporary, and auditable.</action>
  <verify>
    <automated>rg "owner|approver|expiration|P07-|two-release|release-block" .planning/phases/07-pc-agent-message-bridge-opencode-plugin/07-WAIVER-TEMPLATE.md .planning/phases/07-pc-agent-message-bridge-opencode-plugin/07-ALIGNMENT-RUNBOOK.md .planning/phases/07-pc-agent-message-bridge-opencode-plugin/07-GAP-CLOSED-CHECKLIST.md</automated>
  </verify>
  <done>Governance controls prevent silent gate bypass and enforce bounded deprecation behavior.</done>
</task>
</tasks>

<verification>
- `npm.cmd --prefix pc-agent-plugin run verify:phase-07`
- `rg "verify:phase-07|pull_request|gateway|pc-agent-plugin" .github/workflows/phase-07-alignment-gate.yml`
- `rg "owner|approver|expiration|P07-|two-release|release-block" .planning/phases/07-pc-agent-message-bridge-opencode-plugin/07-WAIVER-TEMPLATE.md .planning/phases/07-pc-agent-message-bridge-opencode-plugin/07-ALIGNMENT-RUNBOOK.md .planning/phases/07-pc-agent-message-bridge-opencode-plugin/07-GAP-CLOSED-CHECKLIST.md`
</verification>

<success_criteria>
- Hard gate is consolidated, executable, and enforced by CI.
- Waiver usage is explicit and time-bounded with required ownership metadata.
- Deprecation governance is documented and linked to checklist enforcement.
</success_criteria>

<output>
After completion, create `.planning/phases/07-pc-agent-message-bridge-opencode-plugin/07-03-SUMMARY.md`
</output>
