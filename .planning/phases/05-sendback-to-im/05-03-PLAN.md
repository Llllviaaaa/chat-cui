---
phase: 05-sendback-to-im
plan: 03
type: execute
wave: 3
depends_on:
  - "02"
files_modified:
  - web-demo/src/lib/types.ts
  - web-demo/src/lib/api.ts
  - web-demo/src/App.tsx
  - web-demo/src/styles.css
  - web-demo/src/App.test.tsx
autonomous: true
requirements:
  - SKL-04
  - IMS-01
  - IMS-03
must_haves:
  truths:
    - Overlay supports full or partial assistant-text selection and single-segment sendback.
    - Send flow includes preview-confirm and light edit before submit.
    - Failure retains draft with one-click retry and actionable code/message feedback.
  artifacts:
    - path: web-demo/src/App.tsx
      provides: selection, preview, confirm, retry sendback UX.
      contains: assistant-only send actions + failure state retention
    - path: web-demo/src/lib/api.ts
      provides: sendback HTTP call and structured error extraction.
      contains: POST /sessions/{session_id}/sendback
    - path: web-demo/src/App.test.tsx
      provides: behavior tests for full send, partial selection, and retry flow.
      contains: SKL-04 + IMS-03 assertions
---

<objective>
Implement Phase-5 sendback UX in web demo.

Purpose: Let user select assistant output, preview/edit, and send to IM with resilient failure handling.
Output: sendback-capable overlay UI and tests.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add sendback API/types support in web demo</name>
  <files>web-demo/src/lib/types.ts, web-demo/src/lib/api.ts</files>
  <behavior>
    - API exposes sendback call with phase-5 request payload.
    - Error parsing returns code/message so UI can show actionable failures.
  </behavior>
  <action>Extend existing API abstraction and type model for sendback operations.</action>
  <verify>
    <automated>npm.cmd --prefix web-demo run test</automated>
  </verify>
  <done>Frontend can call backend sendback endpoint with structured responses.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add overlay selection, preview-confirm, and retry UX</name>
  <files>web-demo/src/App.tsx, web-demo/src/styles.css, web-demo/src/App.test.tsx</files>
  <behavior>
    - Full-block and partial-text (assistant-only) selection can open sendback preview.
    - Preview allows light editing and confirm send.
    - Failed send keeps draft and supports one-click retry with code/message.
  </behavior>
  <action>Implement sendback interaction state in overlay with top+inline failure prompts.</action>
  <verify>
    <automated>npm.cmd --prefix web-demo run test</automated>
  </verify>
  <done>Phase-5 user-facing interaction loop is demo-visible and test-covered.</done>
</task>
</tasks>

<output>
After completion, create `.planning/phases/05-sendback-to-im/05-03-SUMMARY.md`
</output>

