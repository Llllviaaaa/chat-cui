---
phase: 08-ai-gateway-skill-service-opencode
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gateway/pom.xml
  - gateway/src/main/java/com/chatcui/gateway/routing/RouteCasResult.java
  - gateway/src/main/java/com/chatcui/gateway/routing/RouteOwnershipRecord.java
  - gateway/src/main/java/com/chatcui/gateway/routing/RouteOwnershipStore.java
  - gateway/src/main/java/com/chatcui/gateway/routing/RouteKeyFactory.java
  - gateway/src/main/java/com/chatcui/gateway/routing/RedisRouteOwnershipStore.java
  - gateway/src/test/java/com/chatcui/gateway/routing/RouteKeyFactoryTest.java
  - gateway/src/test/java/com/chatcui/gateway/routing/RedisRouteOwnershipStoreTest.java
autonomous: true
requirements:
  - P08-ROUTE-01
  - P08-FENCE-01
user_setup:
  - Provide a Redis 6.2+ endpoint for gateway test/runtime profiles (for example `CHATCUI_REDIS_URL`).
must_haves:
  truths:
    - Session route truth is persisted in Redis by `tenant_id + session_id`, not in local in-memory maps.
    - Route ownership record contains both owners (`skill_service_owner`, `gateway_owner`) and a monotonic `route_version`.
    - Owner migration updates route + fence atomically; stale version writes fail deterministically.
    - Fenced owner can be resolved from route record immediately after CAS success.
  artifacts:
    - path: gateway/src/main/java/com/chatcui/gateway/routing/RouteOwnershipStore.java
      provides: route-table read/write contract with CAS and fence semantics.
      contains: load/upsert/casTransfer APIs and deterministic conflict result model
    - path: gateway/src/main/java/com/chatcui/gateway/routing/RedisRouteOwnershipStore.java
      provides: Redis-backed route truth implementation.
      contains: Lua or transaction-based atomic CAS+fence update path
    - path: gateway/src/main/java/com/chatcui/gateway/routing/RouteOwnershipRecord.java
      provides: canonical route record fields.
      contains: tenant_id, session_id, route_version, skill owner, gateway owner, fenced owner
    - path: gateway/src/test/java/com/chatcui/gateway/routing/RedisRouteOwnershipStoreTest.java
      provides: regression proof for version conflict and fence behavior.
      contains: CAS success/conflict/fenced-owner test cases
  key_links:
    - from: gateway/src/main/java/com/chatcui/gateway/routing/RouteKeyFactory.java
      to: gateway/src/main/java/com/chatcui/gateway/routing/RedisRouteOwnershipStore.java
      via: Redis key generation with `tenant_id + session_id` and cluster-safe hash tags
      pattern: route key format reused across all route load/update operations
---

<objective>
Establish Redis-backed route ownership as the source of truth with atomic CAS migration and owner fence support.

Purpose: Remove single-process route assumptions so multi-instance gateway routing can be deterministic and conflict-safe.
Output: Route store contracts + Redis implementation + tests proving versioned owner migration and fence semantics.
</objective>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/08-ai-gateway-skill-service-opencode/08-CONTEXT.md
@.planning/phases/08-ai-gateway-skill-service-opencode/08-RESEARCH.md
@gateway/src/main/java/com/chatcui/gateway/runtime/ResumeCoordinator.java
@gateway/src/main/java/com/chatcui/gateway/runtime/ResumeDecision.java
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Define route ownership contracts and Redis key conventions</name>
  <files>gateway/pom.xml, gateway/src/main/java/com/chatcui/gateway/routing/RouteOwnershipRecord.java, gateway/src/main/java/com/chatcui/gateway/routing/RouteOwnershipStore.java, gateway/src/main/java/com/chatcui/gateway/routing/RouteCasResult.java, gateway/src/main/java/com/chatcui/gateway/routing/RouteKeyFactory.java, gateway/src/test/java/com/chatcui/gateway/routing/RouteKeyFactoryTest.java</files>
  <behavior>
    - Route key is anchored on `tenant_id + session_id`; include Redis Cluster-safe hash-tag format so related keys remain co-slotted.
    - `route_version` is numeric and monotonic; contract must represent version-conflict outcome explicitly.
    - Record model includes complete route target (`skill-service owner + gateway owner`) and fenced owner metadata.
  </behavior>
  <action>Add gateway Redis dependency and introduce route ownership interfaces/records so downstream relay and recovery code can consume one stable contract.</action>
  <verify>
    <automated>.\\mvnw.cmd -pl gateway "-Dtest=RouteKeyFactoryTest" test</automated>
  </verify>
  <done>Route key and record contracts compile with deterministic field definitions and test coverage for key layout.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement atomic CAS migration + immediate fence semantics in Redis route store</name>
  <files>gateway/src/main/java/com/chatcui/gateway/routing/RedisRouteOwnershipStore.java, gateway/src/test/java/com/chatcui/gateway/routing/RedisRouteOwnershipStoreTest.java</files>
  <behavior>
    - Use one atomic Redis mutation (Lua script or WATCH/MULTI guarded equivalent) to update owner pair, increment `route_version`, and persist fenced owner.
    - Conflict path must return deterministic result with current `route_version` and active owner metadata.
    - Fence becomes effective immediately after CAS success; no split-brain acceptance window is allowed.
  </behavior>
  <action>Implement Redis route-table persistence with CAS transfer API and tests for success, conflict, and fence visibility.</action>
  <verify>
    <automated>.\\mvnw.cmd -pl gateway "-Dtest=RedisRouteOwnershipStoreTest,RouteKeyFactoryTest" test</automated>
  </verify>
  <done>Gateway has a tested Redis route truth implementation that enforces versioned owner migration and immediate fencing semantics.</done>
</task>
</tasks>

<verification>
- `.\\mvnw.cmd -pl gateway "-Dtest=RouteKeyFactoryTest,RedisRouteOwnershipStoreTest" test`
- `rg "route_version|skill_owner|gateway_owner|fenced_owner|tenant_id|session_id" gateway/src/main/java/com/chatcui/gateway/routing/*.java`
</verification>

<success_criteria>
- Redis route table is the authoritative source for session ownership.
- CAS migration is atomic and rejects stale route updates deterministically.
- Fence metadata is persisted and test-proven immediately on successful migration.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ai-gateway-skill-service-opencode/08-01-SUMMARY.md`
</output>
