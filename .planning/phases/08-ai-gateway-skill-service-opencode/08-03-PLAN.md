---
phase: 08-ai-gateway-skill-service-opencode
plan: 03
type: execute
wave: 3
depends_on:
  - "02"
files_modified:
  - gateway/src/main/java/com/chatcui/gateway/relay/DeliveryAckStateMachine.java
  - gateway/src/main/java/com/chatcui/gateway/relay/UnknownOwnerRecoveryWorker.java
  - gateway/src/main/java/com/chatcui/gateway/runtime/BridgePersistencePublisher.java
  - gateway/src/main/java/com/chatcui/gateway/runtime/ResumeCoordinator.java
  - gateway/src/main/java/com/chatcui/gateway/runtime/ResumeDecision.java
  - gateway/src/test/java/com/chatcui/gateway/relay/DeliveryAckStateMachineTest.java
  - gateway/src/test/java/com/chatcui/gateway/persistence/DeliveryRetryQueueTest.java
  - gateway/src/test/java/com/chatcui/gateway/runtime/ResumeCoordinatorTest.java
  - skill-service/src/main/java/com/chatcui/skill/relay/RelayEventConsumer.java
autonomous: true
requirements:
  - P08-ACK-01
  - P08-RECOVERY-01
  - P08-FENCE-01
must_haves:
  truths:
    - Delivery result uses two deterministic stages: `gateway_owner_accepted` and `client_delivered` or `client_delivery_timeout`.
    - Unknown-owner routing does not fail immediately; it enters short-lived replay/recheck within a fixed 15-minute window.
    - Exceeding replay window returns deterministic terminal envelope including `error_code`, `next_action`, `trace_id`, and `route_version`.
    - Fenced owner explicitly rejects with `OWNER_FENCED`, and upstream reroute semantics are deterministic.
  artifacts:
    - path: gateway/src/main/java/com/chatcui/gateway/relay/DeliveryAckStateMachine.java
      provides: two-stage delivery acknowledgement contract and state transitions.
      contains: stage1 accepted, stage2 delivered/timeout branches
    - path: gateway/src/main/java/com/chatcui/gateway/relay/UnknownOwnerRecoveryWorker.java
      provides: replay-window bounded recovery worker.
      contains: unknown-owner queue processing, route recheck loop, 15-minute expiry branch
    - path: gateway/src/main/java/com/chatcui/gateway/runtime/ResumeCoordinator.java
      provides: fenced-owner rejection integrated with route version decisions.
      contains: `OWNER_FENCED` terminal decision path
    - path: gateway/src/test/java/com/chatcui/gateway/relay/DeliveryAckStateMachineTest.java
      provides: proof of ack state transition correctness.
      contains: accepted->delivered, accepted->timeout, and deterministic envelope assertions
  key_links:
    - from: gateway/src/main/java/com/chatcui/gateway/relay/DeliveryAckStateMachine.java
      to: gateway/src/main/java/com/chatcui/gateway/runtime/BridgePersistencePublisher.java
      via: route_version-aware ack/failure envelope fields
      pattern: failure payload always includes route_version + next_action
    - from: gateway/src/main/java/com/chatcui/gateway/relay/UnknownOwnerRecoveryWorker.java
      to: gateway/src/main/java/com/chatcui/gateway/runtime/ResumeCoordinator.java
      via: route recheck + fence decision loop
      pattern: unknown owner replay stops once owner resolved or replay window expires
    - from: skill-service/src/main/java/com/chatcui/skill/relay/RelayEventConsumer.java
      to: gateway/src/main/java/com/chatcui/gateway/relay/DeliveryAckStateMachine.java
      via: stage transitions reported from relay consumption
      pattern: stage-1 and stage-2 status events are emitted in order
    - from: gateway/src/main/java/com/chatcui/gateway/runtime/ResumeCoordinator.java
      to: gateway/src/main/java/com/chatcui/gateway/routing/RedisRouteOwnershipStore.java
      via: Redis route/fence ownership load before owner-fenced decision
      pattern: coordinator decisions use persisted route truth instead of local owner map only
---

<objective>
Add deterministic two-stage delivery acknowledgement, bounded unknown-owner recovery, and strict fence rejection semantics.

Purpose: Ensure distributed delivery is recoverable and auditable without silent failure, infinite retry, or split-owner writes.
Output: Ack state machine + replay-window worker + fenced-owner decision path and regression tests.
</objective>

<context>
@.planning/phases/08-ai-gateway-skill-service-opencode/08-02-PLAN.md
@.planning/phases/08-ai-gateway-skill-service-opencode/08-CONTEXT.md
@gateway/src/main/java/com/chatcui/gateway/runtime/ResumeCoordinator.java
@gateway/src/main/java/com/chatcui/gateway/persistence/DeliveryRetryQueue.java
@gateway/src/main/java/com/chatcui/gateway/persistence/model/SkillTurnForwardEvent.java
@gateway/src/main/java/com/chatcui/gateway/routing/RedisRouteOwnershipStore.java
@skill-service/src/main/java/com/chatcui/skill/relay/RelayEventConsumer.java
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement two-stage delivery acknowledgement state machine</name>
  <files>gateway/src/main/java/com/chatcui/gateway/relay/DeliveryAckStateMachine.java, gateway/src/main/java/com/chatcui/gateway/runtime/BridgePersistencePublisher.java, gateway/src/test/java/com/chatcui/gateway/relay/DeliveryAckStateMachineTest.java</files>
  <behavior>
    - Stage 1 marks `gateway_owner_accepted` once owner gateway accepts relay unit.
    - Stage 2 marks `client_delivered` on success, otherwise `client_delivery_timeout`.
    - Timeout and terminal branches return deterministic envelope fields: `error_code`, `next_action`, `trace_id`, `route_version`.
  </behavior>
  <action>Add explicit state machine and wire it into gateway relay flow so caller-visible delivery status is no longer single-stage or ambiguous.</action>
  <verify>
    <automated>.\\mvnw.cmd -pl gateway "-Dtest=DeliveryAckStateMachineTest,BridgePersistencePublisherTest" test</automated>
  </verify>
  <done>Gateway exposes stable two-stage delivery outcomes and deterministic timeout failure envelopes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add unknown-owner short-queue recovery with 15-minute replay window</name>
  <files>gateway/src/main/java/com/chatcui/gateway/relay/UnknownOwnerRecoveryWorker.java, skill-service/src/main/java/com/chatcui/skill/relay/RelayEventConsumer.java, gateway/src/test/java/com/chatcui/gateway/persistence/DeliveryRetryQueueTest.java</files>
  <behavior>
    - Unknown/expired owner routes enqueue into short-lived recovery stream rather than immediate terminal failure.
    - Recovery worker rechecks route ownership and retries dispatch within a strict 15-minute replay window.
    - On replay-window expiry, emit deterministic terminal branch (`ROUTE_REPLAY_WINDOW_EXPIRED`) and stop retrying.
  </behavior>
  <action>Implement bounded unknown-owner recovery mechanics using existing Redis retry-queue contracts plus route recheck loop, preserving at-least-once + dedupe guarantees.</action>
  <verify>
    <automated>.\\mvnw.cmd -pl "gateway,skill-service" "-Dtest=DeliveryRetryQueueTest" test</automated>
  </verify>
  <done>Unknown-owner flows are retryable for 15 minutes and then terminate deterministically without infinite loops.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Enforce `OWNER_FENCED` rejection and resume-anchor migration continuity</name>
  <files>gateway/src/main/java/com/chatcui/gateway/runtime/ResumeCoordinator.java, gateway/src/main/java/com/chatcui/gateway/runtime/ResumeDecision.java, gateway/src/test/java/com/chatcui/gateway/runtime/ResumeCoordinatorTest.java</files>
  <behavior>
    - Fenced owner must reject incoming relay/publish attempts with `OWNER_FENCED` and route_version diagnostics.
    - Resume continuation on new owner keeps Phase-6 anchor tuple semantics (`session_id + turn_id + seq`) intact.
    - Old owner is frozen after fence activation; no parallel continuation branch is allowed.
  </behavior>
  <action>Extend resume coordinator decisions to consume route/fence data from Redis ownership state and produce deterministic fenced-owner rejection behavior.</action>
  <verify>
    <automated>.\\mvnw.cmd -pl gateway "-Dtest=ResumeCoordinatorTest,DeliveryAckStateMachineTest" test</automated>
  </verify>
  <done>Fence migration path is deterministic, old owner is blocked, and continuation resumes safely on the new owner anchor.</done>
</task>
</tasks>

<verification>
- `.\\mvnw.cmd -pl "gateway,skill-service" "-Dtest=DeliveryAckStateMachineTest,DeliveryRetryQueueTest,ResumeCoordinatorTest" test`
- `rg "OWNER_FENCED|route_version|gateway_owner_accepted|client_delivered|client_delivery_timeout|ROUTE_REPLAY_WINDOW_EXPIRED" gateway/src/main/java/com/chatcui/gateway skill-service/src/main/java/com/chatcui/skill`
</verification>

<success_criteria>
- Two-stage delivery acknowledgements are emitted and test-validated end-to-end.
- Unknown-owner retry/recovery respects the 15-minute replay window and deterministic terminal branch.
- Fenced-owner behavior is explicit and blocks stale-owner writes immediately.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ai-gateway-skill-service-opencode/08-03-SUMMARY.md`
</output>
