---
phase: 08-ai-gateway-skill-service-opencode
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - gateway/src/main/java/com/chatcui/gateway/relay/RelayEnvelope.java
  - gateway/src/main/java/com/chatcui/gateway/relay/RelayPublisher.java
  - gateway/src/main/java/com/chatcui/gateway/relay/RedisRelayPublisher.java
  - gateway/src/main/java/com/chatcui/gateway/runtime/BridgePersistencePublisher.java
  - gateway/src/test/java/com/chatcui/gateway/runtime/BridgePersistencePublisherTest.java
  - skill-service/src/main/java/com/chatcui/skill/relay/RelayEventConsumer.java
  - skill-service/src/main/java/com/chatcui/skill/relay/RelayDispatchService.java
  - skill-service/src/test/java/com/chatcui/skill/integration/CrossInstanceRelayIntegrationTest.java
autonomous: true
requirements:
  - P08-RELAY-01
  - P08-DEDUPE-01
must_haves:
  truths:
    - A non-target gateway instance does not deliver directly to client; it relays to the event bus first.
    - Relay first hop always targets the `skill-service owner`, then dispatches to the route-selected `gateway/client`.
    - Relay partitioning/order is keyed by `tenant_id + session_id` for per-session ordering guarantees.
    - Duplicate events are suppressed using `session_id + turn_id + seq + topic` across every relay hop.
  artifacts:
    - path: gateway/src/main/java/com/chatcui/gateway/relay/RedisRelayPublisher.java
      provides: gateway-side first-hop relay publish implementation.
      contains: stream/topic publish keyed by tenant/session and route_version metadata
    - path: skill-service/src/main/java/com/chatcui/skill/relay/RelayEventConsumer.java
      provides: first-hop relay consume and owner-aware dispatch trigger.
      contains: consumer-group processing, dedupe guard, and ack path
    - path: skill-service/src/main/java/com/chatcui/skill/relay/RelayDispatchService.java
      provides: second-hop dispatch from skill owner to target gateway/client.
      contains: route lookup usage and deterministic dispatch target mapping
    - path: skill-service/src/test/java/com/chatcui/skill/integration/CrossInstanceRelayIntegrationTest.java
      provides: cross-instance relay proof.
      contains: gatewayA ingress -> skill owner consume -> gatewayB/client delivery assertions
  key_links:
    - from: gateway/src/main/java/com/chatcui/gateway/runtime/BridgePersistencePublisher.java
      to: gateway/src/main/java/com/chatcui/gateway/relay/RedisRelayPublisher.java
      via: non-target route branch
      pattern: publish relay envelope instead of local direct-forward branch
    - from: skill-service/src/main/java/com/chatcui/skill/relay/RelayEventConsumer.java
      to: skill-service/src/main/java/com/chatcui/skill/relay/RelayDispatchService.java
      via: owner-hop processing pipeline
      pattern: first-hop consume calls second-hop dispatch only after dedupe guard
    - from: gateway/src/main/java/com/chatcui/gateway/relay/RedisRelayPublisher.java
      to: skill-service/src/main/java/com/chatcui/skill/relay/RelayEventConsumer.java
      via: relay envelope dedupe tuple (`session_id + turn_id + seq + topic`)
      pattern: producer and consumer enforce the same dedupe tuple across the first hop
---

<objective>
Implement cross-instance relay wiring that always routes through skill-service owner first, then to the target gateway/client, with full-path dedupe.

Purpose: Guarantee correct destination delivery when OpenCode messages arrive on non-target gateway instances.
Output: Gateway relay publisher + skill-service relay consumer/dispatcher + integration tests proving distributed hop flow and dedupe.
</objective>

<context>
@.planning/phases/08-ai-gateway-skill-service-opencode/08-01-PLAN.md
@.planning/phases/08-ai-gateway-skill-service-opencode/08-CONTEXT.md
@.planning/phases/08-ai-gateway-skill-service-opencode/08-RESEARCH.md
@gateway/src/main/java/com/chatcui/gateway/runtime/BridgePersistencePublisher.java
@gateway/src/main/java/com/chatcui/gateway/persistence/SkillPersistenceForwarder.java
@skill-service/src/main/java/com/chatcui/skill/service/TurnPersistenceService.java
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add gateway first-hop relay path for non-target owner routing</name>
  <files>gateway/src/main/java/com/chatcui/gateway/relay/RelayEnvelope.java, gateway/src/main/java/com/chatcui/gateway/relay/RelayPublisher.java, gateway/src/main/java/com/chatcui/gateway/relay/RedisRelayPublisher.java, gateway/src/main/java/com/chatcui/gateway/runtime/BridgePersistencePublisher.java, gateway/src/test/java/com/chatcui/gateway/runtime/BridgePersistencePublisherTest.java</files>
  <behavior>
    - Route lookup determines if current gateway is target owner; if not target, publish relay envelope instead of local delivery.
    - Relay envelope carries `tenant_id`, `session_id`, `turn_id`, `seq`, `topic`, `trace_id`, `route_version`, and hop metadata.
    - Relay partition key is `tenant_id + session_id`; do not use fire-and-forget Pub/Sub for this path.
    - Dedupe key for relay publish and receive follows `session_id + turn_id + seq + topic`.
  </behavior>
  <action>Extend gateway publish flow to support first-hop event-bus relay while preserving existing resume-anchor guards and deterministic reason envelopes.</action>
  <verify>
    <automated>.\\mvnw.cmd -pl gateway "-Dtest=BridgePersistencePublisherTest" test</automated>
  </verify>
  <done>Gateway emits first-hop relay events only when it is not the target owner, and duplicate relay emissions are suppressed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement skill-service owner consume-and-dispatch second-hop pipeline</name>
  <files>skill-service/src/main/java/com/chatcui/skill/relay/RelayEventConsumer.java, skill-service/src/main/java/com/chatcui/skill/relay/RelayDispatchService.java, skill-service/src/test/java/com/chatcui/skill/integration/CrossInstanceRelayIntegrationTest.java</files>
  <behavior>
    - Skill-service consumes first-hop relay events via consumer-group pattern and only proceeds after dedupe guard.
    - Dispatch path uses route ownership record (`skill owner + gateway owner`) to forward to the target gateway/client.
    - Relay ack to stream happens after consume/dispatch handling path is resolved; failures keep message in pending/recovery lane.
  </behavior>
  <action>Add redis-stream consumer + dispatch service in skill-service to complete the locked relay topology (`gateway -> skill owner -> target gateway/client`).</action>
  <verify>
    <automated>.\\mvnw.cmd -pl "gateway,skill-service" "-Dtest=BridgePersistencePublisherTest,CrossInstanceRelayIntegrationTest" test</automated>
  </verify>
  <done>Cross-instance relay works end-to-end with ordered per-session processing and full-path dedupe semantics.</done>
</task>
</tasks>

<verification>
- `.\\mvnw.cmd -pl "gateway,skill-service" "-Dtest=BridgePersistencePublisherTest,CrossInstanceRelayIntegrationTest" test`
- `rg "session_id\\s*\\+.*turn_id\\s*\\+.*seq\\s*\\+.*topic|route_version|skill-service owner|gateway owner" gateway/src/main/java/com/chatcui/gateway skill-service/src/main/java/com/chatcui/skill`
</verification>

<success_criteria>
- Non-target gateway paths are relayed through the event bus and never mis-delivered locally.
- Skill-service owner first-hop/second-hop routing is test-proven in integration flow.
- End-to-end dedupe tuple is consistent across gateway and skill-service relay components.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ai-gateway-skill-service-opencode/08-02-SUMMARY.md`
</output>
