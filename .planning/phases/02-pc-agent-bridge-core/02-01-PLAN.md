---
phase: 02-pc-agent-bridge-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pc-agent-plugin/src/core/bridge/ProtocolBridge.ts
  - pc-agent-plugin/test/contracts/ProtocolBridge.contract.test.ts
autonomous: true
requirements:
  - BRG-03
must_haves:
  truths:
    - Core bridge maps command/response/error contracts without field loss for required metadata.
    - Stream event contract supports delta/final/completed semantics with deterministic mapping.
    - Unknown event/version mismatch handling is explicit and structured.
  artifacts:
    - path: pc-agent-plugin/src/core/bridge/ProtocolBridge.ts
      provides: Core protocol conversion for OpenCode and gateway event contracts.
      contains: request/delta/final/completed/error conversion paths
    - path: pc-agent-plugin/test/contracts/ProtocolBridge.contract.test.ts
      provides: Contract tests for command/response/error and compatibility behavior.
      contains: unsupported/version mismatch/extension preservation assertions
---

<objective>
Deliver stable protocol conversion contracts in shared core for BRG-03.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Upgrade bridge schema and conversion logic</name>
  <files>pc-agent-plugin/src/core/bridge/ProtocolBridge.ts</files>
  <action>Implement typed bridge event schema for request, delta, final, completed, and error. Preserve unknown fields under extensions and emit structured mismatch errors for unsupported/version conflicts.</action>
  <verify>
    <automated>npm --prefix pc-agent-plugin run test:contracts</automated>
  </verify>
  <done>ProtocolBridge covers command/response/error contracts with deterministic conversion behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend contract tests for stream and failure semantics</name>
  <files>pc-agent-plugin/test/contracts/ProtocolBridge.contract.test.ts</files>
  <action>Add tests for request mapping, delta/final/completed roundtrip, unknown event unsupported mapping, version mismatch, and extensions preservation.</action>
  <verify>
    <automated>npm --prefix pc-agent-plugin run test:contracts</automated>
  </verify>
  <done>Contract suite verifies BRG-03 behaviors and blocks schema drift.</done>
</task>

</tasks>

<output>
After completion, create `.planning/phases/02-pc-agent-bridge-core/02-01-SUMMARY.md`
</output>
