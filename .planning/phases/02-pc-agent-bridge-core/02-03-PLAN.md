---
phase: 02-pc-agent-bridge-core
plan: 03
type: execute
wave: 3
depends_on:
  - "02"
files_modified:
  - pc-agent-plugin/src/host-adapter/HostEventBridge.ts
  - pc-agent-plugin/src/host-adapter/contracts/HostPluginContract.ts
  - pc-agent-plugin/src/cli/commands/runSession.ts
  - pc-agent-plugin/test/integration/host/HostEventContract.integration.test.ts
  - pc-agent-plugin/test/integration/cli/CliRealChain.integration.test.ts
autonomous: true
requirements:
  - BRG-01
  - BRG-02
must_haves:
  truths:
    - Host adapter lifecycle and event bridge are compatible with phase-2 stream contracts.
    - CLI adapter continues using shared runtime and can drive one session stream request path.
    - Adapter modules remain thin and do not implement conversion logic locally.
  artifacts:
    - path: pc-agent-plugin/src/host-adapter/HostEventBridge.ts
      provides: Host-side compatibility with phase-2 stream and error events.
      contains: request, stream delta/final/completed, error event dispatch paths
    - path: pc-agent-plugin/src/cli/commands/runSession.ts
      provides: CLI one-session bridge command path over shared runtime.
      contains: request send, stream handling markers, deterministic failure behavior
    - path: pc-agent-plugin/test/integration/host/HostEventContract.integration.test.ts
      provides: Host integration tests for stream event compatibility.
      contains: request->stream->completion path assertions
---

<objective>
Align host and CLI adapters with phase-2 bridge-core runtime behavior for BRG-01/02.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update host event bridge contract for stream semantics</name>
  <files>pc-agent-plugin/src/host-adapter/contracts/HostPluginContract.ts, pc-agent-plugin/src/host-adapter/HostEventBridge.ts, pc-agent-plugin/test/integration/host/HostEventContract.integration.test.ts</files>
  <action>Adapt host event in/out paths to phase-2 request/stream/completion/error contracts while preserving adapter thinness.</action>
  <verify>
    <automated>npm --prefix pc-agent-plugin run test:host-events && npm --prefix pc-agent-plugin run test:host-lifecycle</automated>
  </verify>
  <done>Host adapter remains thin and is contract-compatible with stream runtime.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Align CLI session command with one-session stream runtime</name>
  <files>pc-agent-plugin/src/cli/commands/runSession.ts, pc-agent-plugin/test/integration/cli/CliRealChain.integration.test.ts</files>
  <action>Ensure CLI run-session drives shared runtime turn request and handles stream completion/error markers under one-session contract.</action>
  <verify>
    <automated>npm --prefix pc-agent-plugin run test:cli-real-chain && npm --prefix pc-agent-plugin run test:cli-bootstrap</automated>
  </verify>
  <done>CLI mode remains shared-core aligned and phase-2 bridge flow compatible.</done>
</task>

</tasks>

<output>
After completion, create `.planning/phases/02-pc-agent-bridge-core/02-03-SUMMARY.md`
</output>
