---
phase: 02-pc-agent-bridge-core
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - pc-agent-plugin/src/core/runtime/BridgeRuntime.ts
  - pc-agent-plugin/src/core/runtime/BridgeRuntimeFactory.ts
  - pc-agent-plugin/src/core/runtime/SessionGatewayTransport.ts
  - pc-agent-plugin/test/integration/core/BridgeSessionRuntime.integration.test.ts
autonomous: true
requirements:
  - BRG-02
  - BRG-03
must_haves:
  truths:
    - Long-connection runtime supports one session stream lifecycle with explicit start/end.
    - Session model enforces single in-flight turn and emits BUSY error for concurrent turn requests.
    - Runtime propagates gateway stream events as ordered plugin events for one session.
  artifacts:
    - path: pc-agent-plugin/src/core/runtime/BridgeRuntime.ts
      provides: Session stream runtime with in-flight state and BUSY handling.
      contains: connect/start/stop/dispose, single in-flight enforcement, ordered stream propagation
    - path: pc-agent-plugin/src/core/runtime/SessionGatewayTransport.ts
      provides: Transport abstraction for long-lived gateway session connections.
      contains: connect/disconnect/send/onMessage contract
    - path: pc-agent-plugin/test/integration/core/BridgeSessionRuntime.integration.test.ts
      provides: Runtime integration evidence for long-connection session behavior.
      contains: stream pass-through, single in-flight, busy and lifecycle assertions
---

<objective>
Implement long-connection session runtime baseline for BRG-02.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add session transport abstraction and runtime stream path</name>
  <files>pc-agent-plugin/src/core/runtime/SessionGatewayTransport.ts, pc-agent-plugin/src/core/runtime/BridgeRuntime.ts, pc-agent-plugin/src/core/runtime/BridgeRuntimeFactory.ts</files>
  <action>Introduce gateway transport abstraction and wire BridgeRuntime to consume/send stream messages through a long-lived connection boundary while preserving existing event model.</action>
  <verify>
    <automated>npm --prefix pc-agent-plugin run test:bridge-runtime</automated>
  </verify>
  <done>Runtime can connect/disconnect and process one-session stream events through transport.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce single in-flight turn and BUSY semantics</name>
  <files>pc-agent-plugin/src/core/runtime/BridgeRuntime.ts, pc-agent-plugin/test/integration/core/BridgeSessionRuntime.integration.test.ts</files>
  <action>Track in-flight turn per session and emit deterministic BUSY structured error when concurrent turn request arrives before completion.</action>
  <verify>
    <automated>npm --prefix pc-agent-plugin run test:bridge-runtime</automated>
  </verify>
  <done>Single in-flight policy is deterministic and covered by integration tests.</done>
</task>

</tasks>

<output>
After completion, create `.planning/phases/02-pc-agent-bridge-core/02-02-SUMMARY.md`
</output>
