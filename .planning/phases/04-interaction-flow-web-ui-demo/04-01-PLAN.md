---
phase: 04-interaction-flow-web-ui-demo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skill-service/src/main/java/com/chatcui/skill/api/SkillDemoTurnController.java
  - skill-service/src/main/java/com/chatcui/skill/api/SkillDemoTurnExceptionHandler.java
  - skill-service/src/main/java/com/chatcui/skill/api/dto/DemoTurnRequest.java
  - skill-service/src/main/java/com/chatcui/skill/api/dto/DemoTurnAcceptedResponse.java
  - skill-service/src/main/java/com/chatcui/skill/service/SkillDemoTurnService.java
  - skill-service/src/test/java/com/chatcui/skill/service/SkillDemoTurnServiceTest.java
autonomous: true
requirements:
  - CMD-03
  - SKL-03
  - DEM-01
must_haves:
  truths:
    - Generate request returns immediate accepted response suitable for in-chat feedback.
    - Demo turn stream persists deterministic delta/final/completed progression for one turn.
    - Session-level turn sequence remains monotonic across follow-up turns.
  artifacts:
    - path: skill-service/src/main/java/com/chatcui/skill/api/SkillDemoTurnController.java
      provides: Demo turn acceptance API for web-demo generate/follow-up actions.
      contains: POST /demo/skill/sessions/{session_id}/turns
    - path: skill-service/src/main/java/com/chatcui/skill/service/SkillDemoTurnService.java
      provides: Prompt acceptance + async persistence simulation for multi-turn demo.
      contains: waiting/running/completed-compatible event progression
    - path: skill-service/src/test/java/com/chatcui/skill/service/SkillDemoTurnServiceTest.java
      provides: Executable proof that accepted turn eventually reaches completed+delivered.
      contains: seq monotonicity and terminal status assertions
  key_links:
    - from: DemoTurnController
      to: SkillDemoTurnService
      via: tenant/client/session/prompt command handoff
      pattern: accepted response returned immediately
    - from: SkillDemoTurnService
      to: TurnPersistenceService
      via: SkillTurnEventRequest persistence path
      pattern: delta -> final -> completed snapshots
---

<objective>
Provide backend support for Phase 4 interaction demo by accepting turns and simulating persisted stream progression.

Purpose: Ensure web-demo can show immediate generate feedback and poll history to observe running/completed transitions.
Output: Demo turn API, validation/error contract, and deterministic persistence behavior test.
</objective>

<context>
@.planning/phases/04-interaction-flow-web-ui-demo/04-CONTEXT.md
@.planning/phases/04-interaction-flow-web-ui-demo/04-RESEARCH.md
@.planning/phases/03-skill-service-persistence-apis/03-VERIFICATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add demo turn request/acceptance API contract</name>
  <files>skill-service/src/main/java/com/chatcui/skill/api/SkillDemoTurnController.java, skill-service/src/main/java/com/chatcui/skill/api/SkillDemoTurnExceptionHandler.java, skill-service/src/main/java/com/chatcui/skill/api/dto/DemoTurnRequest.java, skill-service/src/main/java/com/chatcui/skill/api/dto/DemoTurnAcceptedResponse.java</files>
  <behavior>
    - POST accepts tenant_id/client_id/prompt payload and session path parameter.
    - Successful response returns accepted status with session_id/turn_id/accepted_at.
    - Invalid request returns deterministic INVALID_REQUEST error schema.
  </behavior>
  <action>Create controller + DTO contracts in snake_case and scoped exception advice for request validation errors.</action>
  <verify>
    <automated>mvnw.cmd -pl skill-service -Dtest=SkillDemoTurnServiceTest test</automated>
  </verify>
  <done>Web-demo can call a deterministic turn acceptance API and render immediate feedback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement demo turn service with deterministic asynchronous event progression</name>
  <files>skill-service/src/main/java/com/chatcui/skill/service/SkillDemoTurnService.java</files>
  <behavior>
    - Service validates required identifiers and prompt.
    - Service persists DELTA immediately and schedules FINAL/COMPLETED updates.
    - Sequence progression remains monotonic within each session.
  </behavior>
  <action>Use per-session sequence state and scheduled persistence callbacks to produce polling-friendly status progression.</action>
  <verify>
    <automated>mvnw.cmd -pl skill-service -Dtest=SkillDemoTurnServiceTest test</automated>
  </verify>
  <done>History endpoint can expose a realistic running -> completed lifecycle for UI demo playback.</done>
</task>

<task type="auto">
  <name>Task 3: Add lifecycle cleanup for demo scheduler</name>
  <files>skill-service/src/main/java/com/chatcui/skill/service/SkillDemoTurnService.java</files>
  <action>Add bean destroy hook that stops scheduled executor to avoid residual worker threads during shutdown.</action>
  <verify>
    <automated>mvnw.cmd -pl skill-service -am test</automated>
  </verify>
  <done>Demo service lifecycle remains clean in test and runtime shutdown paths.</done>
</task>

</tasks>

<verification>
- `mvnw.cmd -pl skill-service -Dtest=SkillDemoTurnServiceTest test`
</verification>

<success_criteria>
- Generate request yields immediate accepted payload for UI card bootstrap.
- Persisted stream state reaches completed lifecycle for each turn.
- Multi-turn in same session maintains stable sequence ordering.
</success_criteria>

<output>
After completion, create `.planning/phases/04-interaction-flow-web-ui-demo/04-01-SUMMARY.md`
</output>

