---
phase: 04-interaction-flow-web-ui-demo
plan: 03
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - web-demo/src/App.tsx
  - web-demo/src/styles.css
  - web-demo/src/App.test.tsx
autonomous: true
requirements:
  - SKL-02
  - SKL-03
  - DEM-01
must_haves:
  truths:
    - Status card expands into a full overlay skill client that covers chat area.
    - Overlay supports multi-turn continuation on same session.
    - Card and overlay states stay synchronized and recover prior reading position on reopen.
  artifacts:
    - path: web-demo/src/App.tsx
      provides: Overlay session view, follow-up input, and card/overlay synchronization logic.
      contains: expand action, disabled chat input while overlay open, polling status convergence
    - path: web-demo/src/App.test.tsx
      provides: Interaction tests for overlay follow-up and card summary synchronization.
      contains: SKL-02/SKL-03 behavioral assertions
  key_links:
    - from: status card expand action
      to: overlay session view
      via: activeSessionId routing
      pattern: preserve session continuity and re-open position
    - from: overlay send action
      to: demo turn API
      via: startTurnForSession with existing session_id
      pattern: follow-up turns update same session history
---

<objective>
Complete expanded skill-session interaction flow and consistency behavior between compact chat card and full overlay client.

Purpose: Satisfy SKL-02/SKL-03 by enabling in-session continuation while preserving single-session UX coherence.
Output: Expandable overlay view, follow-up turn input, conflict routing, and synchronization tests.
</objective>

<context>
@.planning/phases/04-interaction-flow-web-ui-demo/04-CONTEXT.md
@.planning/phases/04-interaction-flow-web-ui-demo/04-RESEARCH.md
@.planning/phases/04-interaction-flow-web-ui-demo/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement expanded overlay skill client and follow-up composer</name>
  <files>web-demo/src/App.tsx, web-demo/src/styles.css, web-demo/src/App.test.tsx</files>
  <behavior>
    - Running/completed card exposes Expand action.
    - Expand opens full overlay over chat timeline/input area.
    - Overlay submit sends follow-up prompt to same session.
  </behavior>
  <action>Add overlay layout, active session routing, and follow-up send behavior using existing session id.</action>
  <verify>
    <automated>npm.cmd --prefix web-demo run test</automated>
  </verify>
  <done>User can continue multi-turn conversation in expanded skill view.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add cross-view sync, one-running-session guard, and scroll restore behavior</name>
  <files>web-demo/src/App.tsx, web-demo/src/App.test.tsx</files>
  <behavior>
    - Card status/summary reflects latest overlay turn outputs.
    - Triggering generate while another session runs prompts route-to-current-session notice.
    - Reopening overlay restores previous scroll position.
  </behavior>
  <action>Implement shared session view model with polling updates, running-session conflict prompt, and per-session scroll state cache.</action>
  <verify>
    <automated>npm.cmd --prefix web-demo run test</automated>
  </verify>
  <done>Compact and expanded surfaces stay consistent for the same session lifecycle.</done>
</task>

</tasks>

<verification>
- `npm.cmd --prefix web-demo run test`
- `npm.cmd --prefix web-demo run build`
</verification>

<success_criteria>
- Overlay continuation flow works without opening a separate native client.
- Multi-turn updates converge to one shared session state in card + overlay.
- Running-session conflict path prevents accidental parallel default sessions.
</success_criteria>

<output>
After completion, create `.planning/phases/04-interaction-flow-web-ui-demo/04-03-SUMMARY.md`
</output>

