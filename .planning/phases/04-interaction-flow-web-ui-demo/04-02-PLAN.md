---
phase: 04-interaction-flow-web-ui-demo
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - web-demo/package.json
  - web-demo/tsconfig.json
  - web-demo/vite.config.ts
  - web-demo/index.html
  - web-demo/src/main.tsx
  - web-demo/src/styles.css
  - web-demo/src/lib/types.ts
  - web-demo/src/lib/api.ts
  - web-demo/src/App.tsx
  - web-demo/src/setupTests.ts
  - web-demo/src/App.test.tsx
autonomous: true
requirements:
  - CMD-01
  - CMD-02
  - CMD-03
  - SKL-01
  - DEM-01
must_haves:
  truths:
    - Slash trigger appears only when `/` is first character of chat input.
    - Trigger panel supports Local OpenCode prompt entry and generate action.
    - Generate creates immediate one-line status card with running feedback.
  artifacts:
    - path: web-demo/src/App.tsx
      provides: Slash-trigger entry and in-chat one-line status card interaction.
      contains: SKILL selector, Local OpenCode option, generate workflow
    - path: web-demo/src/lib/api.ts
      provides: HTTP adapter for demo turn start and history polling calls.
      contains: POST /demo/skill/sessions/{session_id}/turns, GET /sessions/{session_id}/history
    - path: web-demo/src/App.test.tsx
      provides: Behavioral tests for slash selector and immediate status-card flow.
      contains: CMD-01/CMD-02/CMD-03/SKL-01 assertions
  key_links:
    - from: App slash panel
      to: HttpSkillDemoApi.startTurn
      via: Generate button action
      pattern: session created then immediate running card
    - from: App status card
      to: HttpSkillDemoApi.getHistory
      via: polling refresh
      pattern: card state resolves from history snapshots
---

<objective>
Create a runnable web-demo shell that demonstrates slash trigger, prompt submit, and immediate status card feedback.

Purpose: Deliver user-visible interaction path for CMD-01/02/03 and SKL-01 without native IM client dependency.
Output: React+Vite demo app with slash panel, API client, status card UI, and baseline tests.
</objective>

<context>
@.planning/phases/04-interaction-flow-web-ui-demo/04-CONTEXT.md
@.planning/phases/04-interaction-flow-web-ui-demo/04-RESEARCH.md
@.planning/phases/04-interaction-flow-web-ui-demo/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold standalone web-demo module and test/build toolchain</name>
  <files>web-demo/package.json, web-demo/tsconfig.json, web-demo/vite.config.ts, web-demo/index.html, web-demo/src/main.tsx, web-demo/src/setupTests.ts</files>
  <action>Create React+Vite TypeScript workspace with Vitest/Testing Library, build script, and DOM test setup.</action>
  <verify>
    <automated>npm.cmd --prefix web-demo run build</automated>
  </verify>
  <done>Web-demo can run independently from native IM packaging and supports CI-friendly tests/builds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement slash trigger selector and generate panel flow</name>
  <files>web-demo/src/App.tsx, web-demo/src/lib/types.ts, web-demo/src/lib/api.ts, web-demo/src/styles.css, web-demo/src/App.test.tsx</files>
  <behavior>
    - Typing `/` reveals SKILL selector with Local OpenCode option.
    - User enters prompt and clicks Generate.
    - Chat timeline receives immediate one-line status card with running state.
  </behavior>
  <action>Implement app state model for trigger panel, session creation, and immediate card insertion; wire API calls for turn start and polling.</action>
  <verify>
    <automated>npm.cmd --prefix web-demo run test</automated>
  </verify>
  <done>Phase-4 entry interaction works end-to-end in browser demo.</done>
</task>

</tasks>

<verification>
- `npm.cmd --prefix web-demo run test`
- `npm.cmd --prefix web-demo run build`
</verification>

<success_criteria>
- Slash interaction path is deterministic and test-covered.
- Generate action produces immediate visible running card.
- Demo app is buildable/testable standalone (`DEM-01` baseline).
</success_criteria>

<output>
After completion, create `.planning/phases/04-interaction-flow-web-ui-demo/04-02-SUMMARY.md`
</output>

