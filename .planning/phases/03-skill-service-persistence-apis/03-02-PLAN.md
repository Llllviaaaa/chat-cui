---
phase: 03-skill-service-persistence-apis
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - skill-service/src/main/java/com/chatcui/skill/service/TurnPersistenceService.java
  - skill-service/src/main/java/com/chatcui/skill/service/SessionHistoryQueryService.java
  - skill-service/src/main/java/com/chatcui/skill/api/SessionHistoryController.java
  - skill-service/src/main/java/com/chatcui/skill/persistence/model/TurnRecord.java
  - skill-service/src/main/java/com/chatcui/skill/persistence/mapper/TurnRecordMapper.java
  - skill-service/src/main/resources/mybatis/TurnRecordMapper.xml
  - skill-service/src/test/java/com/chatcui/skill/service/TurnPersistenceServiceTest.java
  - skill-service/src/test/java/com/chatcui/skill/api/SessionHistoryControllerIntegrationTest.java
autonomous: true
requirements:
  - SVC-02
  - SVC-03
must_haves:
  truths:
    - Skill service persists incremental turn snapshots from stream events without creating duplicate deliveries.
    - Sequence guards prevent older seq updates from regressing newer turn state.
    - Session history API returns deterministic ascending turn order with turn-based pagination.
  artifacts:
    - path: skill-service/src/main/java/com/chatcui/skill/service/TurnPersistenceService.java
      provides: Idempotent write path for delta/final/completed/error event updates.
      contains: monotonic seq guard and delivery_status transitions
    - path: skill-service/src/main/resources/mybatis/TurnRecordMapper.xml
      provides: SQL for upsert/update and ordered history retrieval.
      contains: ON DUPLICATE KEY UPDATE and ORDER BY created_at ASC, turn_id ASC
    - path: skill-service/src/main/java/com/chatcui/skill/api/SessionHistoryController.java
      provides: Session-resource GET history endpoint with pagination baseline.
      contains: GET /sessions/{session_id}/history
  key_links:
    - from: SessionHistoryController
      to: SessionHistoryQueryService
      via: controller service delegation
      pattern: GET history request mapped to query command object
    - from: TurnPersistenceService
      to: TurnRecordMapper
      via: mapper upsert/update calls
      pattern: idempotency key session_id + turn_id + seq
    - from: TurnRecordMapper query
      to: API response DTO
      via: deterministic sorting and cursor mapping
      pattern: ascending replay-safe list
---

<objective>
Implement Skill service write/read behavior on top of phase-3 contracts.

Purpose: Make Skill service the source of truth for turn history persistence and retrieval.
Output: Turn upsert service, deterministic history query API, and tests proving SVC-02/SVC-03 behavior.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-skill-service-persistence-apis/03-CONTEXT.md
@.planning/phases/03-skill-service-persistence-apis/03-RESEARCH.md
@.planning/phases/03-skill-service-persistence-apis/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement idempotent turn persistence service with monotonic seq guard</name>
  <files>skill-service/src/main/java/com/chatcui/skill/service/TurnPersistenceService.java, skill-service/src/main/java/com/chatcui/skill/persistence/model/TurnRecord.java, skill-service/src/main/java/com/chatcui/skill/persistence/mapper/TurnRecordMapper.java, skill-service/src/main/resources/mybatis/TurnRecordMapper.xml, skill-service/src/test/java/com/chatcui/skill/service/TurnPersistenceServiceTest.java</files>
  <behavior>
    - Test 1: Replayed event with same session_id/turn_id/seq does not create duplicate state.
    - Test 2: Incoming lower seq does not overwrite a newer snapshot.
    - Test 3: Status transitions in_progress -> completed/error update content snapshot and delivery_status.
  </behavior>
  <action>Write persistence tests first, then implement mapper SQL and service logic using DB-enforced idempotency (`session_id + turn_id + seq`) and `seq` monotonic guards. Persist required metadata and snapshot content per locked decisions.</action>
  <verify>
    <automated>mvnw.cmd -pl skill-service -Dtest=TurnPersistenceServiceTest test</automated>
  </verify>
  <done>Turn write behavior is deterministic, idempotent, and metadata-complete for SVC-02.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deterministic session history query service with turn-based cursor</name>
  <files>skill-service/src/main/java/com/chatcui/skill/service/SessionHistoryQueryService.java, skill-service/src/main/resources/mybatis/TurnRecordMapper.xml, skill-service/src/test/java/com/chatcui/skill/api/SessionHistoryControllerIntegrationTest.java</files>
  <behavior>
    - Test 1: History endpoint returns turns in ascending replay-safe order.
    - Test 2: Cursor pagination returns next turns without skipping or duplication.
    - Test 3: Response items include status, delivery_status, snapshot, trace_id, and seq.
  </behavior>
  <action>Implement query service and mapper query with deterministic ordering (`created_at ASC, turn_id ASC`) and turn-based cursor semantics. Keep field naming in snake_case and avoid offset-only pagination behavior.</action>
  <verify>
    <automated>mvnw.cmd -pl skill-service -Dtest=SessionHistoryControllerIntegrationTest test</automated>
  </verify>
  <done>History read path satisfies replay ordering and baseline pagination expectations for SVC-03.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire REST session history controller to query service contracts</name>
  <files>skill-service/src/main/java/com/chatcui/skill/api/SessionHistoryController.java, skill-service/src/test/java/com/chatcui/skill/api/SessionHistoryControllerIntegrationTest.java</files>
  <behavior>
    - Test 1: GET `/sessions/{session_id}/history` validates required tenant/client/session inputs.
    - Test 2: Successful response includes ordered turns and cursor metadata.
    - Test 3: Invalid cursor or unknown session returns deterministic error contract.
  </behavior>
  <action>Implement controller endpoint and request/response binding around the query service. Keep response JSON snake_case and return explicit error payload for invalid inputs and unavailable sessions.</action>
  <verify>
    <automated>mvnw.cmd -pl skill-service -Dtest=SessionHistoryControllerIntegrationTest test</automated>
  </verify>
  <done>REST contract is callable by future skill client and maps cleanly to deterministic query behavior.</done>
</task>

</tasks>

<verification>
- `mvnw.cmd -pl skill-service -am test`
- Confirm persistence and query tests cover idempotency, ordering, and pagination cases.
</verification>

<success_criteria>
- Turn snapshots are persisted with required metadata and monotonic event updates.
- History API provides replay-safe ascending output with turn-based pagination.
- SVC-02 and SVC-03 are executable via skill-service test suite.
</success_criteria>

<output>
After completion, create `.planning/phases/03-skill-service-persistence-apis/03-02-SUMMARY.md`
</output>
