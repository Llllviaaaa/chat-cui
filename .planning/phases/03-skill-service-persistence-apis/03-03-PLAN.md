---
phase: 03-skill-service-persistence-apis
plan: 03
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - gateway/pom.xml
  - gateway/src/main/java/com/chatcui/gateway/persistence/model/SkillTurnForwardEvent.java
  - gateway/src/main/java/com/chatcui/gateway/persistence/SkillPersistenceForwarder.java
  - gateway/src/main/java/com/chatcui/gateway/persistence/DeliveryRetryQueue.java
  - gateway/src/main/java/com/chatcui/gateway/persistence/DeliveryStatusReporter.java
  - gateway/src/main/java/com/chatcui/gateway/runtime/BridgePersistencePublisher.java
  - gateway/src/test/java/com/chatcui/gateway/persistence/SkillPersistenceForwarderTest.java
  - gateway/src/test/java/com/chatcui/gateway/persistence/DeliveryRetryQueueTest.java
  - gateway/src/test/java/com/chatcui/gateway/runtime/BridgePersistencePublisherTest.java
autonomous: true
requirements:
  - SVC-01
  - SVC-02
must_haves:
  truths:
    - Gateway forwards stream/output events to Skill service near real time using existing snake_case fields.
    - Skill service unavailability does not block session flow and triggers bounded async retry.
    - Delivery status transitions are reported (`pending`, `saved`, `failed`) for query visibility.
  artifacts:
    - path: gateway/src/main/java/com/chatcui/gateway/persistence/SkillPersistenceForwarder.java
      provides: Gateway client/service for forwarding stream events to skill persistence API.
      contains: immediate receive ack and persistence outcome handling split
    - path: gateway/src/main/java/com/chatcui/gateway/persistence/DeliveryRetryQueue.java
      provides: Non-blocking retry policy for skill service outages.
      contains: bounded retry attempts and backoff scheduling
    - path: gateway/src/main/java/com/chatcui/gateway/runtime/BridgePersistencePublisher.java
      provides: Wiring from gateway stream topics to persistence forwarder.
      contains: topic filter for delta/final/completed/error events
  key_links:
    - from: stream topic events
      to: SkillPersistenceForwarder
      via: BridgePersistencePublisher dispatch
      pattern: skill.turn.delta|final|completed|error forward path
    - from: SkillPersistenceForwarder
      to: DeliveryRetryQueue
      via: async enqueue on transport failure
      pattern: non-blocking retry, no session termination
    - from: DeliveryStatusReporter
      to: persisted history visibility
      via: delivery_status transition publish
      pattern: pending->saved|failed lifecycle
---

<objective>
Implement gateway-to-skill persistence forwarding semantics for Phase 3.

Purpose: Satisfy SVC-01 by making gateway transparently forward stream/output events with non-blocking retry and delivery-state visibility.
Output: Gateway forwarder, retry queue, and stream wiring tests aligned to locked delivery semantics.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-skill-service-persistence-apis/03-CONTEXT.md
@.planning/phases/03-skill-service-persistence-apis/03-RESEARCH.md
@pc-agent-plugin/src/core/bridge/ProtocolBridge.ts
@pc-agent-plugin/src/core/events/PluginEvents.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create gateway forwarding contract and Skill persistence client</name>
  <files>gateway/pom.xml, gateway/src/main/java/com/chatcui/gateway/persistence/model/SkillTurnForwardEvent.java, gateway/src/main/java/com/chatcui/gateway/persistence/SkillPersistenceForwarder.java, gateway/src/test/java/com/chatcui/gateway/persistence/SkillPersistenceForwarderTest.java</files>
  <behavior>
    - Test 1: Forwarder posts stream event payload with unchanged snake_case identifiers.
    - Test 2: Forward request emits immediate accepted state independent from persistence result.
    - Test 3: Duplicate idempotency tuple (session_id, turn_id, seq) does not create duplicate downstream save attempts.
  </behavior>
  <action>Add gateway dependencies needed for HTTP forwarding and implement forward event model + client service for Skill API ingest endpoint. Honor locked dual-stage semantics (receive ack separated from persistence outcome).</action>
  <verify>
    <automated>mvnw.cmd -pl gateway -Dtest=SkillPersistenceForwarderTest test</automated>
  </verify>
  <done>Gateway can build and test a deterministic forward request path to skill persistence API.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement bounded async retry queue and delivery status reporter</name>
  <files>gateway/src/main/java/com/chatcui/gateway/persistence/DeliveryRetryQueue.java, gateway/src/main/java/com/chatcui/gateway/persistence/DeliveryStatusReporter.java, gateway/src/test/java/com/chatcui/gateway/persistence/DeliveryRetryQueueTest.java</files>
  <behavior>
    - Test 1: Skill API outage enqueues retry without blocking caller thread.
    - Test 2: Retry attempts cap at configured maximum and marks final failed state.
    - Test 3: Successful retry transitions delivery_status from pending to saved.
  </behavior>
  <action>Implement asynchronous retry component with bounded attempts/backoff and explicit delivery_status transitions. Do not fail-fast the session path and do not silently drop failed persistence events.</action>
  <verify>
    <automated>mvnw.cmd -pl gateway -Dtest=DeliveryRetryQueueTest test</automated>
  </verify>
  <done>Gateway persistence failures follow non-blocking retry policy with explicit status visibility.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire stream topic publisher to persistence forwarder</name>
  <files>gateway/src/main/java/com/chatcui/gateway/runtime/BridgePersistencePublisher.java, gateway/src/test/java/com/chatcui/gateway/runtime/BridgePersistencePublisherTest.java</files>
  <behavior>
    - Test 1: `skill.turn.delta/final/completed/error` events are forwarded to persistence.
    - Test 2: Non-persistence topics are ignored and do not enqueue retries.
    - Test 3: Publisher emits pending status before forward completion.
  </behavior>
  <action>Introduce runtime publisher that subscribes to gateway stream events and forwards only persistence-relevant topics to the forwarder. Keep auth boundary untouched and preserve trace_id/session_id correlation.</action>
  <verify>
    <automated>mvnw.cmd -pl gateway -Dtest=BridgePersistencePublisherTest test</automated>
  </verify>
  <done>SVC-01 forwarding semantics are wired to stream topics with tested status transitions.</done>
</task>

</tasks>

<verification>
- `mvnw.cmd -pl gateway test`
- Confirm forwarding tests cover dual-stage ack, retry, and delivery_status lifecycle.
</verification>

<success_criteria>
- Gateway forwards all required stream/output events to skill-service boundary.
- Skill unavailability behavior is async retry (non-blocking) with no silent drops.
- Delivery status semantics are explicit and test-covered for client visibility.
</success_criteria>

<output>
After completion, create `.planning/phases/03-skill-service-persistence-apis/03-03-SUMMARY.md`
</output>
