---
phase: 03-skill-service-persistence-apis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pom.xml
  - skill-service/pom.xml
  - skill-service/src/main/java/com/chatcui/skill/SkillServiceApplication.java
  - skill-service/src/main/resources/application.yml
  - skill-service/src/main/java/com/chatcui/skill/api/dto/SkillTurnEventRequest.java
  - skill-service/src/main/java/com/chatcui/skill/api/dto/SessionHistoryResponse.java
  - skill-service/src/test/java/com/chatcui/skill/api/dto/SkillTurnEventDtoContractTest.java
  - skill-service/src/main/resources/db/migration/V1__skill_turn_tables.sql
  - skill-service/src/test/java/com/chatcui/skill/persistence/SkillTurnSchemaCompatibilityTest.java
autonomous: true
requirements:
  - SVC-02
  - SVC-03
must_haves:
  truths:
    - Skill service module boots on project baseline stack (JDK 21, Spring Boot, MVC, MyBatis, MySQL).
    - Stream-aligned DTO contracts exist for gateway ingest and history responses with snake_case payloads.
    - MySQL 5.7-safe tables and indexes exist for turn snapshot persistence and delivery idempotency.
  artifacts:
    - path: skill-service/pom.xml
      provides: Skill service dependency and test baseline.
      contains: spring-boot-starter-web, mybatis-spring-boot-starter, mysql-connector-j
    - path: skill-service/src/main/java/com/chatcui/skill/api/dto/SkillTurnEventRequest.java
      provides: Event ingest contract carrying required stream metadata.
      contains: tenant_id, client_id, session_id, turn_id, seq, trace_id, actor, event_type
    - path: skill-service/src/main/resources/db/migration/V1__skill_turn_tables.sql
      provides: Turn snapshot and delivery idempotency schema.
      contains: UNIQUE KEY on session_id + turn_id + seq and ascending history indexes
  key_links:
    - from: gateway stream payload
      to: skill-service ingest DTOs
      via: JSON snake_case field mapping
      pattern: session_id/turn_id/seq/trace_id pass-through
    - from: turn snapshot table
      to: history query API
      via: created_at + turn_id deterministic sort
      pattern: ORDER BY created_at ASC, turn_id ASC
---

<objective>
Bootstrap the Skill service foundation for persistence and history APIs.

Purpose: Establish executable module, contracts, and schema before write/query logic.
Output: New `skill-service` module with DTO contracts and MySQL 5.7-compatible migration baseline.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-skill-service-persistence-apis/03-CONTEXT.md
@.planning/phases/03-skill-service-persistence-apis/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create skill-service Maven module and runtime scaffold</name>
  <files>pom.xml, skill-service/pom.xml, skill-service/src/main/java/com/chatcui/skill/SkillServiceApplication.java, skill-service/src/main/resources/application.yml</files>
  <action>Add `skill-service` to root modules and create Spring Boot + MVC + MyBatis + MySQL baseline module. Keep package root `com.chatcui.skill` and configure datasource/mybatis placeholders (no embedded DB fallback).</action>
  <verify>
    <automated>mvnw.cmd -pl skill-service -am test -DskipTests</automated>
  </verify>
  <done>`skill-service` module resolves and compiles from root Maven build with expected baseline dependencies.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define stream ingest and history DTO contracts in snake_case</name>
  <files>skill-service/src/main/java/com/chatcui/skill/api/dto/SkillTurnEventRequest.java, skill-service/src/main/java/com/chatcui/skill/api/dto/SessionHistoryResponse.java, skill-service/src/test/java/com/chatcui/skill/api/dto/SkillTurnEventDtoContractTest.java</files>
  <behavior>
    - Test 1: DTO serialization keeps snake_case identifiers required by stream contract.
    - Test 2: History item includes turn status and delivery_status fields.
    - Test 3: Actor/event enums accept baseline values user/assistant/system/plugin and delta/final/completed/error.
  </behavior>
  <action>Write DTO contract tests first, then implement request/response DTOs and enums. Preserve locked metadata fields and status literals; avoid camelCase aliases.</action>
  <verify>
    <automated>mvnw.cmd -pl skill-service -Dtest=SkillTurnEventDtoContractTest test</automated>
  </verify>
  <done>DTO contracts are executable and enforce required field names and baseline literals.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add MySQL 5.7 migration for turn snapshot and delivery idempotency</name>
  <files>skill-service/src/main/resources/db/migration/V1__skill_turn_tables.sql, skill-service/src/test/java/com/chatcui/skill/persistence/SkillTurnSchemaCompatibilityTest.java</files>
  <behavior>
    - Test 1: Schema applies cleanly on MySQL 5.7 and creates required tables/indexes.
    - Test 2: Delivery idempotency unique key enforces session_id + turn_id + seq uniqueness.
    - Test 3: History query sort columns are indexed for ascending replay order.
  </behavior>
  <action>Create migration SQL with `skill_turn_snapshot` and `skill_turn_delivery` tables, MySQL 5.7-safe column lengths/indexes, and schema compatibility test that validates DDL + key presence.</action>
  <verify>
    <automated>mvnw.cmd -pl skill-service -Dtest=SkillTurnSchemaCompatibilityTest test</automated>
  </verify>
  <done>Schema and index baseline is reproducible and validated against MySQL 5.7 constraints.</done>
</task>

</tasks>

<verification>
- `mvnw.cmd -pl skill-service -am test`
- Migration test confirms idempotency key and ordered history indexes exist.
</verification>

<success_criteria>
- Skill-service module is buildable from root and follows project backend stack.
- DTO contracts and schema enforce locked phase-3 identifiers and statuses.
- Phase 3 downstream plans can build write/read logic without redefining contracts.
</success_criteria>

<output>
After completion, create `.planning/phases/03-skill-service-persistence-apis/03-01-SUMMARY.md`
</output>
