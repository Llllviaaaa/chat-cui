# 01.1 Acceptance Evidence

## Scope

This record tracks required evidence for Phase 01.1 completion:

- Plugin mode lifecycle/event path
- CLI mode session path
- Real chain gating behavior (OpenCode + AI-Gateway)
- AUTH_V1 compatibility and deterministic failure signaling

## Evidence Log

| Area | Command | Date | Result | Notes |
| --- | --- | --- | --- | --- |
| Plugin mode lifecycle | `npm.cmd --prefix pc-agent-plugin run test:host-lifecycle` | 2026-03-03 | PASS | Validated `init/start/stop/dispose` and reconnect event propagation |
| Plugin mode event contract | `npm.cmd --prefix pc-agent-plugin run test:host-events` | 2026-03-03 | PASS | Host event in/out mapping and unsupported-event error path |
| Plugin mode integration | `npm.cmd --prefix pc-agent-plugin run test:plugin-mode-integration` | 2026-03-03 | PASS | Combined host integration suite passed |
| CLI bootstrap | `npm.cmd --prefix pc-agent-plugin run test:cli-bootstrap` | 2026-03-03 | PASS | Shared runtime factory and AUTH_V1 signing path validated |
| CLI mode real-chain guard | `npm.cmd --prefix pc-agent-plugin run test:cli-real-chain` | 2026-03-03 | PASS | Enforced `CHATCUI_REAL_CHAIN=true` and endpoint presence (no mock-only shortcut) |
| No-drift gate | `npm.cmd --prefix pc-agent-plugin run verify:no-drift` | 2026-03-03 | PASS | Adapter ownership boundaries and parity architecture checks passed |

## Manual Real-Chain Execution Template

Use this command for end-to-end real chain verification after OpenCode and AI-Gateway endpoints are reachable:

```powershell
npm.cmd --prefix pc-agent-plugin run cli -- run-session --question "phase-01.1 real chain probe"
```

Capture and attach:

1. Timestamp
2. Session ID
3. Trace ID
4. OpenCode endpoint
5. AI-Gateway endpoint
6. AUTH_V1 outcome (success or deterministic failure code)

## Current Sign-off

- Plugin mode: ready
- CLI mode: ready
- real chain gating: enforced
- AUTH_V1 compatibility: preserved
