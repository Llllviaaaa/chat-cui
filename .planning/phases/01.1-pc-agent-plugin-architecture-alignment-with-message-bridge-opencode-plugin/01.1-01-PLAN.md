---
phase: 01.1-pc-agent-plugin-architecture-alignment-with-message-bridge-opencode-plugin
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pc-agent-plugin/package.json
  - pc-agent-plugin/tsconfig.json
  - pc-agent-plugin/src/core/bridge/ProtocolBridge.ts
  - pc-agent-plugin/src/core/events/PluginEvents.ts
  - pc-agent-plugin/src/core/lifecycle/LifecycleStateMachine.ts
  - pc-agent-plugin/src/core/runtime/BridgeRuntime.ts
  - pc-agent-plugin/src/core/runtime/BridgeRuntimeFactory.ts
  - pc-agent-plugin/test/architecture/PluginFirstArchitecture.test.ts
autonomous: true
requirements:
  - P01.1-ARCH-01
  - P01.1-ARCH-02
  - P01.1-DRIFT-01
must_haves:
  truths:
    - The runtime architecture is plugin-first and does not reintroduce standalone-app-first runtime ownership.
    - A shared bridge core exists with lifecycle state machine and protocol conversion contracts owned in core/bridge.
    - Drift guards fail fast when host or CLI adapters try to implement bridge conversion logic directly.
  artifacts:
    - path: pc-agent-plugin/src/core/runtime/BridgeRuntimeFactory.ts
      provides: Single factory used by all runtime adapters to create bridge runtime.
      contains: createBridgeRuntime
    - path: pc-agent-plugin/src/core/lifecycle/LifecycleStateMachine.ts
      provides: Canonical init/start/stop/dispose and reconnect transition model.
      contains: init, start, stop, dispose, onReconnect
    - path: pc-agent-plugin/test/architecture/PluginFirstArchitecture.test.ts
      provides: Architecture tests enforcing adapter-thinness and no conversion logic outside core.
      contains: import-boundary and ownership assertions
  key_links:
    - from: pc-agent-plugin/src/core/runtime/BridgeRuntimeFactory.ts
      to: pc-agent-plugin/src/core/runtime/BridgeRuntime.ts
      via: Factory creates one shared runtime contract consumed by all adapters.
      pattern: createBridgeRuntime
    - from: pc-agent-plugin/test/architecture/PluginFirstArchitecture.test.ts
      to: pc-agent-plugin/src/core/bridge/ProtocolBridge.ts
      via: Tests enforce protocol conversion ownership at core layer.
      pattern: protocol conversion
---

<objective>
Create the plugin-first architecture baseline with enforceable no-drift boundaries.

Purpose: Lock architecture direction before implementation to prevent drift away from the plugin bridge identity.
Output: Shared core contracts and architecture drift tests that downstream plans must consume.
</objective>

<execution_context>
@C:/Users/15721/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/15721/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.1-pc-agent-plugin-architecture-alignment-with-message-bridge-opencode-plugin/01.1-CONTEXT.md
@.planning/phases/01.1-pc-agent-plugin-architecture-alignment-with-message-bridge-opencode-plugin/01.1-RESEARCH.md

<requirement_map>
Stable provisional IDs for all 01.1 plans:
- P01.1-ARCH-01 -> BRG-01 intent (plugin-first runtime identity)
- P01.1-ARCH-02 -> BRG-01/BRG-03 intent (layered topology and lifecycle contract)
- P01.1-ARCH-03 -> BRG-01 intent (plugin mode + CLI mode on one shared core)
- P01.1-CHAIN-01 -> BRG-02/BRG-03 intent (CLI mode must run real OpenCode + gateway chain)
- P01.1-COMPAT-01 -> AUT-02/AUT-03 intent (AUTH_V1 compatibility)
- P01.1-SEC-01 -> AUT-01 intent (secure credential semantics parity)
- P01.1-MIG-01 -> BRG-01 intent (legacy Java runtime cutover from main path)
- P01.1-DRIFT-01 -> BRG-01/AUT-* intent (no architecture drift gates)
</requirement_map>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create plugin-first core scaffold and contracts</name>
  <files>pc-agent-plugin/package.json, pc-agent-plugin/tsconfig.json, pc-agent-plugin/src/core/runtime/BridgeRuntime.ts, pc-agent-plugin/src/core/runtime/BridgeRuntimeFactory.ts, pc-agent-plugin/src/core/lifecycle/LifecycleStateMachine.ts, pc-agent-plugin/src/core/events/PluginEvents.ts, pc-agent-plugin/src/core/bridge/ProtocolBridge.ts</files>
  <action>Initialize a plugin runtime workspace with layered core contracts only. Define lifecycle, runtime-factory, and protocol bridge interfaces in core modules. Do not implement host-specific SDK calls or CLI argument parsing in this task. Keep protocol conversion ownership in `src/core/bridge` only.</action>
  <verify>
    <automated>npm --prefix pc-agent-plugin run test:contracts</automated>
  </verify>
  <done>Core contracts compile and represent plugin-first architecture boundaries with no standalone-app-first structure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add no-drift architecture tests for ownership and dependency boundaries</name>
  <files>pc-agent-plugin/test/architecture/PluginFirstArchitecture.test.ts</files>
  <behavior>
    - Host and CLI adapters are forbidden from implementing protocol conversion logic directly.
    - Adapters are required to instantiate runtime through BridgeRuntimeFactory.
    - Core bridge modules must not import host SDK or CLI parsing modules.
  </behavior>
  <action>Create architecture tests that fail on import-boundary violations and conversion-ownership drift. Treat violations as blocking failures for this phase.</action>
  <verify>
    <automated>npm --prefix pc-agent-plugin run test:architecture</automated>
  </verify>
  <done>No-drift tests are executable and fail when plugin-first ownership boundaries are broken.</done>
</task>

</tasks>

<verification>
Validate that all future 01.1 execution work is anchored to a plugin-first core and cannot drift into split logic across adapters.
</verification>

<success_criteria>
Plugin-first contracts and no-drift checks exist and are executable inputs for downstream plans.
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-pc-agent-plugin-architecture-alignment-with-message-bridge-opencode-plugin/01.1-01-SUMMARY.md`
</output>
