---
phase: 01.1-pc-agent-plugin-architecture-alignment-with-message-bridge-opencode-plugin
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - pc-agent-plugin/src/core/auth/AuthFailureMapper.ts
  - pc-agent-plugin/src/core/auth/AuthPayloadBuilder.ts
  - pc-agent-plugin/src/core/auth/AuthSigner.ts
  - pc-agent-plugin/src/core/security/CredentialErrors.ts
  - pc-agent-plugin/src/core/security/CredentialProvider.ts
  - pc-agent-plugin/src/core/security/WindowsCredentialProvider.ts
  - pc-agent-plugin/test/unit/auth/AuthErrorContract.test.ts
  - pc-agent-plugin/test/unit/auth/AuthSignerContract.test.ts
  - pc-agent-plugin/test/unit/security/WindowsCredentialProvider.test.ts
autonomous: true
requirements:
  - P01.1-ARCH-02
  - P01.1-COMPAT-01
  - P01.1-SEC-01
must_haves:
  truths:
    - Plugin runtime builds AUTH_V1 payload/signature exactly as documented and remains gateway-compatible.
    - Authentication failure handling in plugin runtime preserves deterministic AUTH_V1 codes and safe messaging expectations.
    - Credential storage and typed error behavior preserve existing secure semantics and no secret leakage.
  artifacts:
    - path: pc-agent-plugin/src/core/auth/AuthSigner.ts
      provides: Canonical AK/SK signer aligned to documented field ordering and signature rules.
      contains: canonical payload generation and HMAC signature
    - path: pc-agent-plugin/src/core/auth/AuthFailureMapper.ts
      provides: Deterministic AUTH_V1 failure mapping for plugin-side handling.
      contains: AUTH_V1_MISSING_CREDENTIAL, AUTH_V1_INVALID_SIGNATURE, AUTH_V1_TIMESTAMP_OUT_OF_WINDOW, AUTH_V1_REPLAY_DETECTED, AUTH_V1_COOLDOWN_ACTIVE, AUTH_V1_CREDENTIAL_DISABLED, AUTH_V1_PERMISSION_DENIED
    - path: pc-agent-plugin/src/core/security/WindowsCredentialProvider.ts
      provides: Credential provider preserving typed secure-storage semantics.
      contains: NOT_FOUND, ACCESS_DENIED, CORRUPT_ENTRY, INVALID_INPUT
    - path: pc-agent-plugin/test/unit/security/WindowsCredentialProvider.test.ts
      provides: Regression tests for secret redaction and typed error behavior.
      contains: no plaintext leakage assertions
  key_links:
    - from: pc-agent-plugin/src/core/auth/AuthPayloadBuilder.ts
      to: docs/auth/aksk-signature-spec.md
      via: Canonical field order and payload rules must match spec.
      pattern: ak, tenant_id, client_id, timestamp, nonce, session_id
    - from: pc-agent-plugin/src/core/auth/AuthFailureMapper.ts
      to: docs/auth/auth-error-contract.md
      via: Plugin failure mapping must preserve documented AUTH_V1 contract.
      pattern: AUTH_V1_
    - from: pc-agent-plugin/src/core/security/WindowsCredentialProvider.ts
      to: pc-agent/src/main/java/com/chatcui/agent/auth/CredentialProvider.java
      via: Typed credential error reason parity during language/runtime migration.
      pattern: NOT_FOUND|ACCESS_DENIED|CORRUPT_ENTRY|INVALID_INPUT
---

<objective>
Implement shared-core auth and security adapters that preserve gateway compatibility and secure credential semantics.

Purpose: Prevent regressions against completed Phase 1 AUTH guarantees while migrating runtime architecture.
Output: AUTH_V1 signer/error mapper and credential provider contracts with compatibility and redaction test coverage.
</objective>

<execution_context>
@C:/Users/15721/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/15721/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.1-pc-agent-plugin-architecture-alignment-with-message-bridge-opencode-plugin/01.1-CONTEXT.md
@.planning/phases/01.1-pc-agent-plugin-architecture-alignment-with-message-bridge-opencode-plugin/01.1-RESEARCH.md
@.planning/phases/01.1-pc-agent-plugin-architecture-alignment-with-message-bridge-opencode-plugin/01.1-01-PLAN.md
@docs/auth/aksk-signature-spec.md
@docs/auth/auth-error-contract.md
@gateway/src/main/java/com/chatcui/gateway/auth/model/AuthRequest.java
@gateway/src/test/java/com/chatcui/gateway/auth/model/AuthFailureCodeContractTest.java
@gateway/src/test/java/com/chatcui/gateway/auth/model/AuthErrorContractDocSyncTest.java
@pc-agent/src/main/java/com/chatcui/agent/auth/CredentialProvider.java
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port canonical AUTH_V1 signing logic into shared core auth package</name>
  <files>pc-agent-plugin/src/core/auth/AuthPayloadBuilder.ts, pc-agent-plugin/src/core/auth/AuthSigner.ts, pc-agent-plugin/test/unit/auth/AuthSignerContract.test.ts</files>
  <behavior>
    - Canonical string field order is exactly `ak`, `tenant_id`, `client_id`, `timestamp`, `nonce`, `session_id`.
    - Payload formatting uses UTF-8 and newline rules matching docs.
    - Signature generation uses HMAC-SHA256 and Base64 output.
  </behavior>
  <action>Implement auth payload builder and signer in shared core only. Reuse Phase 1 gateway expectations as fixtures to prevent drift. Do not duplicate signing logic in host or CLI adapters.</action>
  <verify>
    <automated>npm --prefix pc-agent-plugin run test:auth-signer</automated>
  </verify>
  <done>Plugin auth signer matches AUTH_V1 canonical contract and tests fail on ordering/encoding drift.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve AUTH_V1 failure mapping and secure credential semantics</name>
  <files>pc-agent-plugin/src/core/auth/AuthFailureMapper.ts, pc-agent-plugin/src/core/security/CredentialErrors.ts, pc-agent-plugin/src/core/security/CredentialProvider.ts, pc-agent-plugin/src/core/security/WindowsCredentialProvider.ts, pc-agent-plugin/test/unit/auth/AuthErrorContract.test.ts, pc-agent-plugin/test/unit/security/WindowsCredentialProvider.test.ts</files>
  <behavior>
    - AUTH failure mapper covers every documented AUTH_V1 code.
    - Credential provider emits typed reasons equivalent to Java reference semantics.
    - Redaction tests confirm no raw secret, SK, or signature leakage in logs/string outputs.
  </behavior>
  <action>Implement deterministic AUTH error mapping and secure credential-provider abstraction in shared core security package. Keep typed error semantics stable and ensure adapter consumers only receive safe messages.</action>
  <verify>
    <automated>npm --prefix pc-agent-plugin run test:auth-errors && npm --prefix pc-agent-plugin run test:security</automated>
  </verify>
  <done>AUTH_V1 error handling and credential semantics are compatible, typed, and redaction-safe.</done>
</task>

</tasks>

<verification>
Confirm plugin auth/security behavior remains contract-compatible with gateway while preserving existing secure credential semantics.
</verification>

<success_criteria>
Shared core contains production-ready AUTH_V1 and credential adapters with compatibility tests proving no Phase 1 regression.
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-pc-agent-plugin-architecture-alignment-with-message-bridge-opencode-plugin/01.1-02-SUMMARY.md`
</output>
