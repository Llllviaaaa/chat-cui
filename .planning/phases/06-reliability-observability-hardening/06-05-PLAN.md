---
phase: 06-reliability-observability-hardening
plan: 05
type: execute
wave: 3
depends_on:
  - "04"
files_modified:
  - gateway/pom.xml
  - gateway/src/main/java/com/chatcui/gateway/observability/BridgeMetricsRegistry.java
  - gateway/src/main/java/com/chatcui/gateway/persistence/DeliveryRetryQueue.java
  - gateway/src/test/java/com/chatcui/gateway/persistence/DeliveryRetryQueueTest.java
  - skill-service/pom.xml
  - skill-service/src/main/java/com/chatcui/skill/observability/SkillMetricsRecorder.java
  - skill-service/src/main/java/com/chatcui/skill/service/SendbackService.java
  - skill-service/src/test/java/com/chatcui/skill/service/SendbackServiceTest.java
  - .planning/phases/06-reliability-observability-hardening/06-OBSERVABILITY-BASELINE.md
autonomous: true
requirements:
  - DEM-02
must_haves:
  truths:
    - Reconnect/resume and persistence/sendback outcomes emit low-cardinality metrics across gateway and skill-service boundaries.
    - Metric labels are restricted to stable dimensions (`component`, `failure_class`, `outcome`, `retryable`) and avoid high-cardinality request identifiers.
    - Operators can use one baseline runbook to map failure classes (`auth|bridge|persistence|sendback|unknown`) to dashboard panels and alert thresholds.
  artifacts:
    - path: gateway/src/main/java/com/chatcui/gateway/observability/BridgeMetricsRegistry.java
      provides: gateway metric names and tagged counters/timers for bridge/persistence reliability outcomes.
      contains: reconnect/resume/persistence meter definitions
    - path: skill-service/src/main/java/com/chatcui/skill/observability/SkillMetricsRecorder.java
      provides: skill-service metric emission for sendback outcomes and failure classes.
      contains: low-cardinality sendback meters wired from service flow
    - path: .planning/phases/06-reliability-observability-hardening/06-OBSERVABILITY-BASELINE.md
      provides: phase-6 observability baseline with metric contract and alert guidance.
      contains: metric names, label rules, dashboard panels, threshold defaults
  key_links:
    - from: gateway/src/main/java/com/chatcui/gateway/persistence/DeliveryRetryQueue.java
      to: gateway/src/main/java/com/chatcui/gateway/observability/BridgeMetricsRegistry.java
      via: retry outcome instrumentation
      pattern: retry transitions increment outcome-tagged gateway metrics
    - from: skill-service/src/main/java/com/chatcui/skill/service/SendbackService.java
      to: skill-service/src/main/java/com/chatcui/skill/observability/SkillMetricsRecorder.java
      via: sendback success/failure/dedup metric emission
      pattern: service outcomes mapped to stable failure_class/outcome tags
---

<objective>
Implement cross-service metrics instrumentation and observability baseline runbook on top of the standardized logging/taxonomy contract.

Purpose: Complete DEM-02 operational visibility with low-cardinality metrics and documented dashboard/alert guidance.
Output: Gateway and skill-service metric wiring plus a phase-local observability baseline document.
</objective>

<context>
@.planning/phases/06-reliability-observability-hardening/06-CONTEXT.md
@.planning/phases/06-reliability-observability-hardening/06-RESEARCH.md
@.planning/phases/06-reliability-observability-hardening/06-04-PLAN.md
@gateway/src/main/java/com/chatcui/gateway/persistence/DeliveryRetryQueue.java
@skill-service/src/main/java/com/chatcui/skill/service/SendbackService.java
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Wire gateway reliability metrics for reconnect/resume and persistence retry outcomes</name>
  <files>gateway/pom.xml, gateway/src/main/java/com/chatcui/gateway/observability/BridgeMetricsRegistry.java, gateway/src/main/java/com/chatcui/gateway/persistence/DeliveryRetryQueue.java, gateway/src/test/java/com/chatcui/gateway/persistence/DeliveryRetryQueueTest.java</files>
  <behavior>
    - Gateway emits counters/timers for reconnect/persistence outcomes using low-cardinality tags only.
    - Delivery retry queue transitions (`pending`, `saved`, `failed`) are reflected in outcome-tagged metrics.
    - Metric names and failure_class tags align with the shared observability taxonomy from plan 06-04.
  </behavior>
  <action>Add gateway metrics dependency and registry wiring, then instrument retry queue transitions to produce stable meter series suitable for dashboards and alerts.</action>
  <verify>
    <automated>mvnw.cmd -pl gateway -Dtest=DeliveryRetryQueueTest test</automated>
  </verify>
  <done>Gateway reliability paths expose deterministic, low-cardinality metric series with test coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Instrument skill-service sendback metrics and publish observability baseline runbook</name>
  <files>skill-service/pom.xml, skill-service/src/main/java/com/chatcui/skill/observability/SkillMetricsRecorder.java, skill-service/src/main/java/com/chatcui/skill/service/SendbackService.java, skill-service/src/test/java/com/chatcui/skill/service/SendbackServiceTest.java, .planning/phases/06-reliability-observability-hardening/06-OBSERVABILITY-BASELINE.md</files>
  <behavior>
    - Sendback flow emits success/failure/dedup metrics tagged by canonical failure_class and retryable semantics.
    - Metrics avoid dynamic identifiers (`trace_id`, raw session ids, payload content) as labels.
    - Baseline runbook documents metric inventory, dashboard panels, and default alert thresholds per failure class.
  </behavior>
  <action>Integrate `SkillMetricsRecorder` into sendback outcome paths and document the final metric/log contract in `06-OBSERVABILITY-BASELINE.md` for rollout operators.</action>
  <verify>
    <automated>mvnw.cmd -pl skill-service -Dtest=SendbackServiceTest test && rg "failure_class|component|outcome|retryable|reconnect|persistence|sendback" .planning/phases/06-reliability-observability-hardening/06-OBSERVABILITY-BASELINE.md</automated>
  </verify>
  <done>Skill-service metrics and baseline documentation make DEM-02 observability actionable for operations teams.</done>
</task>
</tasks>

<verification>
- `mvnw.cmd -pl gateway -Dtest=DeliveryRetryQueueTest test`
- `mvnw.cmd -pl skill-service -Dtest=SendbackServiceTest test`
- `rg "failure_class|component|outcome|retryable|reconnect|persistence|sendback" .planning/phases/06-reliability-observability-hardening/06-OBSERVABILITY-BASELINE.md`
</verification>

<success_criteria>
- Gateway and skill-service emit low-cardinality reliability metrics aligned to shared taxonomy values.
- Observability baseline document defines metric names/tags/panels/thresholds for auth/bridge/persistence/sendback classes.
- DEM-02 is covered with both structured logs (plan 06-04) and actionable metrics/runbook outputs.
</success_criteria>

<output>
After completion, create `.planning/phases/06-reliability-observability-hardening/06-05-SUMMARY.md`
</output>
