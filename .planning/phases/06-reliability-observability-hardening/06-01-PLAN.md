---
phase: 06-reliability-observability-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pc-agent-plugin/src/core/events/PluginEvents.ts
  - pc-agent-plugin/src/core/runtime/SessionGatewayTransport.ts
  - pc-agent-plugin/src/core/runtime/BridgeRuntime.ts
  - pc-agent-plugin/src/host-adapter/contracts/HostPluginContract.ts
  - pc-agent-plugin/src/host-adapter/HostEventBridge.ts
  - pc-agent-plugin/test/integration/core/BridgeSessionRuntime.integration.test.ts
  - pc-agent-plugin/test/integration/host/HostEventContract.integration.test.ts
  - pc-agent-plugin/test/integration/cli/CliRealChain.integration.test.ts
autonomous: true
requirements:
  - BRG-04
must_haves:
  truths:
    - Runtime auto-reconnects with bounded exponential backoff plus jitter and fresh auth material on every attempt.
    - Runtime emits phased reconnect states (`reconnecting`, `resumed`, `failed`) with deterministic terminal reason code and `next_action`.
    - Post-reconnect stream handling drops duplicate seq events and triggers compensation flow for seq gaps before normal continuation.
  artifacts:
    - path: pc-agent-plugin/src/core/runtime/BridgeRuntime.ts
      provides: reconnect coordinator, resume-anchor handling, and sequence anomaly policy.
      contains: reconnect loop + duplicate drop + gap compensation trigger
    - path: pc-agent-plugin/src/core/runtime/SessionGatewayTransport.ts
      provides: transport contract for fresh-auth reconnect and resume-anchor operations.
      contains: reconnect/resume contract typed around session_id + turn_id + seq
    - path: pc-agent-plugin/test/integration/core/BridgeSessionRuntime.integration.test.ts
      provides: executable proof for reconnect/resume ordering behavior.
      contains: bounded attempts + resumed/failed + duplicate/gap assertions
  key_links:
    - from: pc-agent-plugin/src/host-adapter/HostEventBridge.ts
      to: pc-agent-plugin/src/core/runtime/BridgeRuntime.ts
      via: runtime.reconnect dispatch
      pattern: host reconnect event invokes single-owner reconnect flow
    - from: pc-agent-plugin/src/core/runtime/BridgeRuntime.ts
      to: pc-agent-plugin/src/core/runtime/SessionGatewayTransport.ts
      via: reconnect + resume invocations
      pattern: every attempt performs fresh auth before resume anchor call
---

<objective>
Implement plugin-side reconnect and resume reliability behavior for transient disconnect recovery.

Purpose: Close BRG-04 on the plugin boundary with deterministic reconnect policy and continuity-safe stream handling.
Output: Updated runtime/host contracts plus reconnect/resume integration tests.
</objective>

<context>
@.planning/phases/06-reliability-observability-hardening/06-CONTEXT.md
@.planning/phases/06-reliability-observability-hardening/06-RESEARCH.md
@pc-agent-plugin/src/core/runtime/BridgeRuntime.ts
@pc-agent-plugin/src/host-adapter/HostEventBridge.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Extend runtime and host contracts for reconnect policy and resume anchor</name>
  <files>pc-agent-plugin/src/core/events/PluginEvents.ts, pc-agent-plugin/src/core/runtime/SessionGatewayTransport.ts, pc-agent-plugin/src/host-adapter/contracts/HostPluginContract.ts, pc-agent-plugin/test/integration/host/HostEventContract.integration.test.ts</files>
  <behavior>
    - Runtime contract includes `reconnecting`, `resumed`, and `failed` reconnect state events.
    - Terminal failure payload carries deterministic reason code, retryable flag, and `next_action`.
    - Reconnect input and resume anchor are typed as `session_id + turn_id + seq` and host dispatch remains backward compatible.
  </behavior>
  <action>Define/expand event and transport interfaces needed by reconnect execution before implementation wiring, including explicit fresh-auth data input per attempt.</action>
  <verify>
    <automated>npm.cmd --prefix pc-agent-plugin run test:host-events</automated>
  </verify>
  <done>Contracts are explicit and test-guarded for reconnect lifecycle and resume-anchor payload shape.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement bounded reconnect coordinator and sequence anomaly policy</name>
  <files>pc-agent-plugin/src/core/runtime/BridgeRuntime.ts, pc-agent-plugin/src/host-adapter/HostEventBridge.ts, pc-agent-plugin/test/integration/core/BridgeSessionRuntime.integration.test.ts, pc-agent-plugin/test/integration/cli/CliRealChain.integration.test.ts</files>
  <behavior>
    - Reconnect flow is single-owner per session and uses bounded exponential backoff with jitter.
    - Every reconnect attempt generates fresh auth payload material before transport connect/resume.
    - Duplicate seq after reconnect is dropped with diagnostic event; seq gap emits compensation signal and pauses normal continuation until handled.
  </behavior>
  <action>Implement reconnect orchestration directly in `BridgeRuntime` and route host bridge events into that flow without introducing parallel reconnect owners.</action>
  <verify>
    <automated>npm.cmd --prefix pc-agent-plugin run test:bridge-runtime && npm.cmd --prefix pc-agent-plugin run test:host-events && npm.cmd --prefix pc-agent-plugin run test:cli-real-chain</automated>
  </verify>
  <done>Plugin runtime can recover or fail fast deterministically while preserving ordered, duplicate-safe continuation semantics.</done>
</task>
</tasks>

<verification>
- `npm.cmd --prefix pc-agent-plugin run test:bridge-runtime`
- `npm.cmd --prefix pc-agent-plugin run test:host-events`
- `npm.cmd --prefix pc-agent-plugin run test:cli-real-chain`
</verification>

<success_criteria>
- Reconnect attempts are bounded, jittered, and always use fresh auth before resume.
- Runtime exposes reconnect phases and deterministic terminal failure contract to upper layers.
- Duplicate/gap anomaly handling behavior is covered by integration tests and preserves session continuity constraints.
</success_criteria>

<output>
After completion, create `.planning/phases/06-reliability-observability-hardening/06-01-SUMMARY.md`
</output>

