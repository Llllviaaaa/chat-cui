---
phase: 06-reliability-observability-hardening
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - skill-service/src/main/resources/db/migration/V3__sendback_idempotency_guard.sql
  - skill-service/src/main/java/com/chatcui/skill/persistence/model/SendbackRecord.java
  - skill-service/src/main/java/com/chatcui/skill/persistence/mapper/SendbackRecordMapper.java
  - skill-service/src/main/resources/mybatis/SendbackRecordMapper.xml
  - skill-service/src/main/java/com/chatcui/skill/service/SendbackService.java
  - skill-service/src/main/resources/application.yml
  - skill-service/src/test/java/com/chatcui/skill/persistence/SkillTurnSchemaCompatibilityTest.java
  - skill-service/src/test/java/com/chatcui/skill/service/SendbackServiceTest.java
  - skill-service/src/test/java/com/chatcui/skill/api/SendbackControllerIntegrationTest.java
autonomous: true
requirements:
  - BRG-04
must_haves:
  truths:
    - Sendback path is duplicate-safe during reconnect/retry windows through server-side idempotency keys.
    - Idempotency key is derived from session context plus content fingerprint, not client-generated randomness.
    - Duplicate requests return deterministic prior outcome and do not trigger second IM send.
  artifacts:
    - path: skill-service/src/main/resources/db/migration/V3__sendback_idempotency_guard.sql
      provides: schema-level idempotency enforcement for sendback records.
      contains: idempotency key column and unique index
    - path: skill-service/src/main/java/com/chatcui/skill/service/SendbackService.java
      provides: duplicate-safe sendback orchestration with deterministic duplicate return contract.
      contains: key derivation + existing-record short-circuit
    - path: skill-service/src/test/java/com/chatcui/skill/service/SendbackServiceTest.java
      provides: executable proof that retry windows do not duplicate IM send operations.
      contains: first-send + duplicate-send assertions
  key_links:
    - from: skill-service/src/main/java/com/chatcui/skill/service/SendbackService.java
      to: skill-service/src/main/java/com/chatcui/skill/persistence/mapper/SendbackRecordMapper.java
      via: find-by-idempotency-key before IM send
      pattern: duplicate key short-circuits IM gateway invocation
    - from: skill-service/src/main/resources/db/migration/V3__sendback_idempotency_guard.sql
      to: skill-service/src/main/resources/mybatis/SendbackRecordMapper.xml
      via: new idempotency query/index usage
      pattern: persistence contract matches schema constraints
---

<objective>
Harden sendback flow with server-side idempotency controls to prevent duplicate IM sends.

Purpose: Fulfill Phase-6 duplicate-control decision for reconnect/retry windows and complete BRG-04 sendback integrity expectation.
Output: DB-backed idempotency schema, mapper/service logic, and regression tests.
</objective>

<context>
@.planning/phases/06-reliability-observability-hardening/06-CONTEXT.md
@.planning/phases/06-reliability-observability-hardening/06-RESEARCH.md
@skill-service/src/main/java/com/chatcui/skill/service/SendbackService.java
@skill-service/src/main/resources/mybatis/SendbackRecordMapper.xml
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add sendback idempotency schema and mapper contracts</name>
  <files>skill-service/src/main/resources/db/migration/V3__sendback_idempotency_guard.sql, skill-service/src/main/java/com/chatcui/skill/persistence/model/SendbackRecord.java, skill-service/src/main/java/com/chatcui/skill/persistence/mapper/SendbackRecordMapper.java, skill-service/src/main/resources/mybatis/SendbackRecordMapper.xml, skill-service/src/test/java/com/chatcui/skill/persistence/SkillTurnSchemaCompatibilityTest.java</files>
  <behavior>
    - Migration adds deterministic idempotency key field and unique index.
    - Mapper supports lookup by idempotency key and existing request replay response.
    - Schema compatibility tests guard new index/key requirements on MySQL 5.7-safe SQL.
  </behavior>
  <action>Create additive migration and mapper contract changes without breaking existing request_id correlation lookup behavior.</action>
  <verify>
    <automated>mvnw.cmd -pl skill-service -Dtest=SkillTurnSchemaCompatibilityTest test</automated>
  </verify>
  <done>Persistence layer can enforce and query sendback idempotency deterministically.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce idempotency in sendback orchestration and API-level behavior</name>
  <files>skill-service/src/main/java/com/chatcui/skill/service/SendbackService.java, skill-service/src/main/resources/application.yml, skill-service/src/test/java/com/chatcui/skill/service/SendbackServiceTest.java, skill-service/src/test/java/com/chatcui/skill/api/SendbackControllerIntegrationTest.java</files>
  <behavior>
    - Service computes idempotency key from stable session context and content fingerprint hash.
    - Duplicate key returns deterministic previously persisted response and skips `ImMessageGateway.send`.
    - Retry semantics remain actionable (`retryable` vs terminal failure mapping) and trace_id continuity is preserved.
  </behavior>
  <action>Update service flow to check existing idempotency record before IM dispatch and add tests for duplicate-safe return behavior across controller/service layers.</action>
  <verify>
    <automated>mvnw.cmd -pl skill-service -Dtest=SendbackServiceTest,SendbackControllerIntegrationTest test</automated>
  </verify>
  <done>Sendback retry/reconnect windows no longer risk duplicate IM messages for same semantic request.</done>
</task>
</tasks>

<verification>
- `mvnw.cmd -pl skill-service -Dtest=SkillTurnSchemaCompatibilityTest,SendbackServiceTest,SendbackControllerIntegrationTest test`
</verification>

<success_criteria>
- Duplicate sendback requests in the same idempotency window return prior result instead of re-sending to IM.
- Schema + mapper + service behavior are all test-covered for idempotency path.
- Existing sendback API compatibility is preserved while adding duplicate suppression guarantees.
</success_criteria>

<output>
After completion, create `.planning/phases/06-reliability-observability-hardening/06-03-SUMMARY.md`
</output>

