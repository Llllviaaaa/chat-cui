---
phase: 06-reliability-observability-hardening
plan: 04
type: execute
wave: 2
depends_on:
  - "01"
  - "02"
  - "03"
files_modified:
  - pc-agent-plugin/src/core/events/PluginEvents.ts
  - pc-agent-plugin/src/cli/commands/runSession.ts
  - pc-agent-plugin/test/integration/cli/CliRealChain.integration.test.ts
  - gateway/src/main/java/com/chatcui/gateway/observability/FailureClass.java
  - gateway/src/main/java/com/chatcui/gateway/persistence/SkillPersistenceForwarder.java
  - gateway/src/test/java/com/chatcui/gateway/persistence/SkillPersistenceForwarderTest.java
  - skill-service/src/main/java/com/chatcui/skill/observability/FailureClass.java
  - skill-service/src/main/java/com/chatcui/skill/service/TurnPersistenceService.java
  - skill-service/src/test/java/com/chatcui/skill/service/TurnPersistenceServiceTest.java
autonomous: true
requirements:
  - DEM-02
must_haves:
  truths:
    - One traceable request can be correlated across plugin, gateway, and skill-service logs using shared required fields and `trace_id`.
    - Failure taxonomy is unified to `auth|bridge|persistence|sendback|unknown` with explicit retryability semantics.
    - Structured failure logs at bridge and persistence boundaries exclude sensitive payload content while retaining actionable metadata.
  artifacts:
    - path: gateway/src/main/java/com/chatcui/gateway/observability/FailureClass.java
      provides: gateway failure taxonomy enum aligned with phase-6 contract.
      contains: auth/bridge/persistence/sendback/unknown
    - path: skill-service/src/main/java/com/chatcui/skill/observability/FailureClass.java
      provides: skill-service failure taxonomy enum aligned with gateway/plugin classes.
      contains: auth/bridge/persistence/sendback/unknown
    - path: pc-agent-plugin/src/cli/commands/runSession.ts
      provides: plugin-side structured failure envelope aligned with required fields.
      contains: trace_id and session tuple propagation into failure logs
  key_links:
    - from: pc-agent-plugin/src/cli/commands/runSession.ts
      to: gateway/src/main/java/com/chatcui/gateway/persistence/SkillPersistenceForwarder.java
      via: shared trace/failure structured fields
      pattern: required metadata fields present across plugin and gateway failure logs
    - from: gateway/src/main/java/com/chatcui/gateway/observability/FailureClass.java
      to: skill-service/src/main/java/com/chatcui/skill/observability/FailureClass.java
      via: shared taxonomy constants
      pattern: failure_class values stay drift-free across services
---

<objective>
Implement cross-service structured logging and taxonomy contract for traceability and deterministic failure classification.

Purpose: Fulfill DEM-02 contract standardization first so downstream metrics/runbook work builds on one stable observability schema.
Output: Unified failure taxonomy constants and required structured failure envelope wiring in plugin/gateway/skill-service runtime paths.
</objective>

<context>
@.planning/phases/06-reliability-observability-hardening/06-CONTEXT.md
@.planning/phases/06-reliability-observability-hardening/06-RESEARCH.md
@pc-agent-plugin/src/cli/commands/runSession.ts
@gateway/src/main/java/com/chatcui/gateway/persistence/SkillPersistenceForwarder.java
@skill-service/src/main/java/com/chatcui/skill/service/SendbackService.java
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Introduce shared failure taxonomy constants and envelope fields</name>
  <files>pc-agent-plugin/src/core/events/PluginEvents.ts, gateway/src/main/java/com/chatcui/gateway/observability/FailureClass.java, skill-service/src/main/java/com/chatcui/skill/observability/FailureClass.java</files>
  <behavior>
    - Canonical taxonomy values are constrained to `auth`, `bridge`, `persistence`, `sendback`, `unknown`.
    - Shared envelope schema exposes required fields: tenant_id/client_id/session_id/turn_id/seq/trace_id/error_code/component/status/failure_class/retryable.
    - Type-level contracts guard against taxonomy drift between services.
  </behavior>
  <action>Define taxonomy enums/contracts first so subsequent wiring uses one explicit source of truth and avoids per-module string divergence.</action>
  <verify>
    <automated>mvnw.cmd -pl gateway,skill-service -Dtest=SkillPersistenceForwarderTest,TurnPersistenceServiceTest test && npm.cmd --prefix pc-agent-plugin run test:cli-real-chain</automated>
  </verify>
  <done>Cross-service observability code has one deterministic taxonomy and envelope contract available for runtime wiring.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire structured logging envelope into plugin/gateway/skill persistence paths</name>
  <files>pc-agent-plugin/src/cli/commands/runSession.ts, pc-agent-plugin/test/integration/cli/CliRealChain.integration.test.ts, gateway/src/main/java/com/chatcui/gateway/persistence/SkillPersistenceForwarder.java, gateway/src/test/java/com/chatcui/gateway/persistence/SkillPersistenceForwarderTest.java, skill-service/src/main/java/com/chatcui/skill/service/TurnPersistenceService.java, skill-service/src/test/java/com/chatcui/skill/service/TurnPersistenceServiceTest.java</files>
  <behavior>
    - Failure logs include required fields: tenant_id/client_id/session_id/turn_id/seq/trace_id/error_code/component/status/failure_class/retryable.
    - Taxonomy values emitted by runtime paths are constrained to canonical enum values.
    - Structured events avoid raw payload/body text and retain only safe diagnostics metadata.
  </behavior>
  <action>Route existing failure/reporting code through the shared envelope in plugin CLI and gateway/skill persistence boundaries, preserving existing control flow while standardizing emitted fields.</action>
  <verify>
    <automated>mvnw.cmd -pl gateway,skill-service -Dtest=SkillPersistenceForwarderTest,TurnPersistenceServiceTest test && npm.cmd --prefix pc-agent-plugin run test:cli-real-chain</automated>
  </verify>
  <done>Runtime failure logs from plugin and persistence paths are schema-aligned and deterministic for cross-service correlation.</done>
</task>
</tasks>

<verification>
- `mvnw.cmd -pl gateway,skill-service -Dtest=SkillPersistenceForwarderTest,TurnPersistenceServiceTest test`
- `npm.cmd --prefix pc-agent-plugin run test:cli-real-chain`
</verification>

<success_criteria>
- DEM-02 traceability contract is standardized across plugin, gateway, and skill-service failure logs.
- Taxonomy values and retryability semantics are deterministic and drift-resistant.
- Structured logs contain required correlation fields without sensitive payload leakage.
</success_criteria>

<output>
After completion, create `.planning/phases/06-reliability-observability-hardening/06-04-SUMMARY.md`
</output>
