---
phase: 06-reliability-observability-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - gateway/src/main/java/com/chatcui/gateway/runtime/ResumeAnchor.java
  - gateway/src/main/java/com/chatcui/gateway/runtime/ResumeCoordinator.java
  - gateway/src/main/java/com/chatcui/gateway/runtime/ResumeDecision.java
  - gateway/src/main/java/com/chatcui/gateway/runtime/BridgePersistencePublisher.java
  - gateway/src/main/java/com/chatcui/gateway/persistence/SkillPersistenceForwarder.java
  - gateway/src/main/java/com/chatcui/gateway/persistence/model/SkillTurnForwardEvent.java
  - gateway/src/test/java/com/chatcui/gateway/runtime/ResumeCoordinatorTest.java
  - gateway/src/test/java/com/chatcui/gateway/runtime/BridgePersistencePublisherTest.java
  - gateway/src/test/java/com/chatcui/gateway/integration/SkillPersistenceForwardingIntegrationTest.java
autonomous: true
requirements:
  - BRG-04
must_haves:
  truths:
    - Gateway can evaluate reconnect resume anchors using `session_id + turn_id + seq` and enforce one reconnect owner per session.
    - Duplicate seq events are dropped with explicit diagnostics, and seq gaps trigger compensation/replay path before downstream continuation.
    - Terminal resume failure responses are deterministic and include reason code plus actionable next step.
  artifacts:
    - path: gateway/src/main/java/com/chatcui/gateway/runtime/ResumeCoordinator.java
      provides: gateway-side resume decision engine for duplicate/gap/continue outcomes.
      contains: single-owner reconnect control + anchor evaluation
    - path: gateway/src/main/java/com/chatcui/gateway/runtime/BridgePersistencePublisher.java
      provides: stream publishing with resume-aware gating before persistence forwarding.
      contains: compensation trigger on gap + duplicate suppression
    - path: gateway/src/test/java/com/chatcui/gateway/runtime/ResumeCoordinatorTest.java
      provides: deterministic behavior tests for resume decision matrix.
      contains: continue/drop/compensate/terminal assertions
  key_links:
    - from: gateway/src/main/java/com/chatcui/gateway/runtime/BridgePersistencePublisher.java
      to: gateway/src/main/java/com/chatcui/gateway/runtime/ResumeCoordinator.java
      via: resume decision before publish
      pattern: inbound tuple must pass anchor rules before forward path
    - from: gateway/src/main/java/com/chatcui/gateway/runtime/BridgePersistencePublisher.java
      to: gateway/src/main/java/com/chatcui/gateway/persistence/SkillPersistenceForwarder.java
      via: allowed continuation publish
      pattern: only continue decisions reach persistence forwarding
---

<objective>
Implement gateway-side resume anchor enforcement and anomaly compensation behavior.

Purpose: Ensure BRG-04 continuity semantics are enforced in gateway stream/persistence boundaries, not only in plugin runtime.
Output: Resume decision engine, publisher integration, and integration test coverage.
</objective>

<context>
@.planning/phases/06-reliability-observability-hardening/06-CONTEXT.md
@.planning/phases/06-reliability-observability-hardening/06-RESEARCH.md
@gateway/src/main/java/com/chatcui/gateway/runtime/BridgePersistencePublisher.java
@gateway/src/main/java/com/chatcui/gateway/persistence/SkillPersistenceForwarder.java
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Create resume-anchor coordinator and deterministic decision contracts</name>
  <files>gateway/src/main/java/com/chatcui/gateway/runtime/ResumeAnchor.java, gateway/src/main/java/com/chatcui/gateway/runtime/ResumeDecision.java, gateway/src/main/java/com/chatcui/gateway/runtime/ResumeCoordinator.java, gateway/src/test/java/com/chatcui/gateway/runtime/ResumeCoordinatorTest.java</files>
  <behavior>
    - Coordinator accepts and tracks last acknowledged tuple per session.
    - Duplicate seq returns a drop decision with diagnostic metadata.
    - Gap seq returns a compensate/replay decision and blocks normal continuation.
  </behavior>
  <action>Introduce isolated runtime coordinator classes so resume policy can be tested independently from persistence transport concerns.</action>
  <verify>
    <automated>mvnw.cmd -pl gateway -Dtest=ResumeCoordinatorTest test</automated>
  </verify>
  <done>Gateway has deterministic, test-covered resume decisions aligned with phase-6 locked rules.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire resume decisions into publish/forward pipeline and integration tests</name>
  <files>gateway/src/main/java/com/chatcui/gateway/runtime/BridgePersistencePublisher.java, gateway/src/main/java/com/chatcui/gateway/persistence/SkillPersistenceForwarder.java, gateway/src/main/java/com/chatcui/gateway/persistence/model/SkillTurnForwardEvent.java, gateway/src/test/java/com/chatcui/gateway/runtime/BridgePersistencePublisherTest.java, gateway/src/test/java/com/chatcui/gateway/integration/SkillPersistenceForwardingIntegrationTest.java</files>
  <behavior>
    - Publish path blocks continuation on gap until compensation is emitted.
    - Duplicate tuples never re-forward to downstream persistence path.
    - Terminal failure decisions surface deterministic reason code and `next_action`.
  </behavior>
  <action>Integrate `ResumeCoordinator` into publisher flow and ensure forwarder behavior stays non-blocking for accepted paths while preserving anomaly controls.</action>
  <verify>
    <automated>mvnw.cmd -pl gateway -Dtest=ResumeCoordinatorTest,BridgePersistencePublisherTest,SkillPersistenceForwardingIntegrationTest test</automated>
  </verify>
  <done>Gateway resume continuity enforcement is active in runtime integration tests and protects ordering/duplication semantics.</done>
</task>
</tasks>

<verification>
- `mvnw.cmd -pl gateway -Dtest=ResumeCoordinatorTest,BridgePersistencePublisherTest,SkillPersistenceForwardingIntegrationTest test`
</verification>

<success_criteria>
- Resume anchor tuple policy is executable and deterministic in gateway code paths.
- Duplicate and gap anomalies are handled according to locked decisions without downstream corruption.
- Failure payloads for terminal resume cases are stable enough for client/UI handling.
</success_criteria>

<output>
After completion, create `.planning/phases/06-reliability-observability-hardening/06-02-SUMMARY.md`
</output>

